// SPDX-License-Identifier: AGPL-3.0-or-later
/**
 * vscode-test suite for the start screen (T-3): the webview's message protocol (AC1, AC2),
 * the language refusal (AC3), the write-ahead marker + resume (AC4), and the end-to-end
 * build against a synthetic fixture, schema-validated (AC5). Filled in step by step as
 * `src/start/panel.ts` and `src/start/build.ts` land — placeholder assertion below is
 * replaced, not left in place, once those exist.
 */

import * as assert from 'node:assert/strict';

suite('build-tutorials start screen (placeholder — filled in with panel.ts/build.ts)', () => {
  test('placeholder', () => {
    assert.ok(true);
  });
});
