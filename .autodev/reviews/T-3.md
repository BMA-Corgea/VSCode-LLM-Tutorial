Verdict: REWORK

# Review — T-3, The start screen, shared skins, and building the plan with resume

**Reviewer:** fresh session, auto-review stage. **Branch:** `feature/T-3-start-screen` @
`2250aa8` (merge-base with `main`: `d317672`, verified with `git merge-base`). **Method:** read
the spec, locate, plan, handoff, and T-1 §5.2/§5.7/§4.2 first; read the full
`git diff main...HEAD -- src test package.json esbuild.mjs` (3109 lines) end to end, file by
file; then independently re-ran every claimed command, re-derived the skin-token census myself
from repo-tour's actual CSS rather than trusting the test, and ran two adversarial probes the
handoff didn't do — an XSS payload and a malformed-message fuzz, both through the real,
compiled render/handler functions (Angle 2) — before writing this file. No code was fixed;
nothing pushed, merged, or run through `tracker.mjs`.

## Headline

This is careful, well-tested work: all 7 ACs are substantively implemented, the 87 tests
(74 unit + 13 vscode-test) really do pass, run twice, under both `DISPLAY=:1` and
`xvfb-run -a`, and several of the suite's more unusual claims — the AC6 fixture test running
through the actual rendered page, the fake-DOM suite really executing the shipped client
script, the skin-token census being derived from repo-tour's real `base.css` rather than a
hand-copied list — check out under independent re-derivation, not just re-reading. The
security-sensitive parts hold up under direct attack: an `<img src=x onerror=alert(1)>` idea
value and a `"><script>` breakout in the repo/target fields both come back fully escaped
through the real `renderStartHtml`, and the CSP is a strict `default-src 'none'` with
nonce-only style/script and no `unsafe-inline`.

That said, three real, independently-confirmed defects and one disclosed-but-real spec-text
miss keep this from a clean pass: (1) the resume-on-activate wiring in `extension.ts` never
clears its own `workspaceState` bookmark, so after the first build ever run in a workspace,
every future window activation loads the whole core and pops a progress notification for
nothing, forever — and this integration path has zero test coverage, unit or vscode-test; (2)
`plan.json`/`request.json` are written with a plain `writeFileSync`, not the temp-file+rename
pattern the write-ahead marker uses two functions away in the same file, so a crash mid-write
can leave a corrupt plan on disk; (3) the message handler does not validate the shape of
messages coming from the webview before acting on them — confirmed by directly feeding
`handleStartMessage` a non-string field and an unrecognized message type through the real
compiled code, both of which throw. None of these is a demonstrated exploit or a fundamentally
broken AC, which is why this is REWORK and not BLOCK — but they are concrete and worth fixing
before merge.

## Angle 1 — Spec conformance (7 ACs)

- **AC1 (the form).** All five controls present (`src/start/view.ts` `bodyHtml`): idea
  textarea, recreate checkbox, repo field + Browse, target field + Browse, three-position dial
  (radios, exactly 3, default `manual`). Validation is real and discriminating, not decorative:
  `test/unit/protocol.test.ts` and the real-webview `test/start.test.ts` both submit an invalid
  request and assert the exact reply, then a valid one and assert `onSubmit` actually fires with
  it. The build button carries no `disabled` attribute anywhere (`grep -rn disabled src/start
  src/webview src/skins.ts` — zero hits): a bad submit always round-trips through
  `form:problems`, never a silently-inert button. Confirmed under a real `vscode.WebviewPanel`,
  not just the pure protocol.
- **AC2 (idea-only honesty, exact text).** The line appears under the idea field, never
  silently declining (`test/unit/start-view.test.ts`, `test/start.test.ts`). **Not quite
  byte-exact against the ticket's own spec text**, though — see the gating finding below;
  this is the one place "exact" doesn't fully hold.
- **AC3 (clone / local path / language refusal).** Verified through the REAL repo-tour digest
  pipeline (`cachedOnly: true`, no stubbing): a synthetic Go-only fixture is refused naming
  "go", a TS fixture is not (`test/unit/build.test.ts`), and the refusal reaches the actual form
  via `StartPanel.reportProblem`, proven end to end with a real `WebviewPanel`
  (`test/start.test.ts` lines 185-239), not just a toast. `detectUnsupportedLanguages`
  (`src/start/build.ts:117-125`) refuses only when **no** source file's language is supported —
  matches the plan's own pseudocode (`.autodev/plans/T-3.md` step 6b) precisely, a reasonable
  reading of the AC's ambiguous "outside TS/JS/TSX/Python" wording (the alternative reading —
  refuse if *any* file is in another language — would refuse almost every real repo, since
  README/YAML/shell files would trip it).
- **AC4 (marker + resume).** The marker write-and-resume *mechanism* is solid and well tested
  (see Angle 3). The *wiring* that's supposed to make it actually fire on a real reload has a
  real bug and zero coverage — see gating finding 1.
- **AC5 (plan.json + schema + cost).** Content is right: schema-validated against repo-tour's
  real `schema/build-plan.schema.json` via a hand-rolled checker mirroring repo-tour's own T-12
  test (`test/unit/build.test.ts` lines 82-289), `request.json` matches byte-for-byte, cost line
  is exactly `"$x.xx, y tokens"` or `"this provider does not report usage"`
  (`src/start/build.ts:298-306`, tested). The *write* is not atomic — see gating finding 2.
- **AC6 (skins).** See Angle 4 — independently re-verified, holds up fully.
- **AC7 (script parse guard).** `test/unit/page.test.ts` parses `renderPage`'s generic output
  with `new Function`, including a negative case (a script that *should* fail to parse actually
  does, proving the guard discriminates). `test/unit/start-view.test.ts` goes further: it
  extracts the actual combined bootstrap+page script and *executes* it against a hand-built
  fake DOM, verifying it reacts correctly to `form:problems`/`field:set`/`skin:set` — this is
  materially stronger than AC7's own bar ("parses"), and the handoff's claim that an early draft
  threw real `ReferenceError`s when `startScript()` was tested in isolation is a plausible,
  specific claim consistent with the code structure (`page.ts`'s bootstrap is only ever
  prepended inside `renderPage`, never available to `startScript()` alone).

## Angle 2 — Security of the webview

- **CSP.** `src/webview/page.ts:70`: `default-src 'none'; style-src 'nonce-…'; script-src
  'nonce-…'` — one nonce per render (`randomBytes(16)`, confirmed fresh per call by
  `page.test.ts`'s "never reuses the previous nonce" test), every `<style>`/`<script>` tag
  carries it, no `unsafe-inline` anywhere in the diff (`grep -rn unsafe-inline src` — zero
  hits). repo-tour's `assets/skins/*.css` reference no `url()` resources at all, so the strict
  `default-src 'none'` costs nothing visually.
- **Escaping — verified against the real render function, not just its unit tests.** Bundled
  the actual `src/start/view.ts`/`src/start/request.ts`/`src/start/protocol.ts` with esbuild
  (no stub, no mock) and ran `renderStartHtml` with `idea = '<img src=x onerror=alert(1)>'` and
  `repo = target = '"><script>alert(2)</script>'`. Output:
  `<textarea id="idea" ...>&lt;img src=x onerror=alert(1)&gt;</textarea>`,
  `value="&quot;&gt;&lt;script&gt;alert(2)&lt;/script&gt;"` for both repo and target, and the
  same escaping in the `problem-idea` div. No raw `<img`/`<script>` anywhere in the output.
  Title, skin labels/notes (`pickerHtml`), and every problem message are escaped the same way
  (`src/webview/html.ts`'s `escapeHtml`/`escapeAttr`, used consistently everywhere user or
  repo-tour-provided text lands in markup).
- **Message-shape validation — a real, confirmed gap.** `handleStartMessage`
  (`src/start/protocol.ts:58-89`) and `StartPanel`'s listener
  (`this.panel.webview.onDidReceiveMessage((message: InboundMessage) => { void
  this.onMessage(message); }` — `src/start/panel.ts:64`) trust the incoming message's shape at
  compile time only; there is no runtime check. Fed the real compiled `handleStartMessage`
  directly:
  - `form:submit` with `{ repo: 12345, target: {evil:true} }` → throws
    `TypeError: repo.trim is not a function` (from `validateRepo`, `src/start/request.ts:68`).
  - an unrecognized `{ type: 'evil:message' }` → throws `unhandled start-screen message: ...`
    (the `default` branch, `src/start/protocol.ts:84-87`).
  - `payload: { request: null }` and a missing `payload` entirely → both throw
    (`Cannot read properties of null/undefined`).
  Any of these, dispatched through the real `onDidReceiveMessage` wiring, becomes an unhandled
  promise rejection rather than a graceful failure. **However**: `pick:repo`/`pick:target`
  carry no payload in the `InboundMessage` type at all and the chosen path always comes from
  `vscode.window.showOpenDialog`'s own result (`src/start/panel.ts:45-53`), never from message
  content — so the specific "pick:target with a crafted path" attack the review angle names is
  structurally impossible here regardless of this gap. Given the escaping above holds and the
  CSP is nonce-only with no external content, I found no actual path for a remote/webview-side
  attacker to *construct* an arbitrary postMessage in the first place; this is a real,
  confirmed defense-in-depth gap (and the review's own rubric names "an unvalidated message" as
  a gating category), not a demonstrated exploit.

## Angle 3 — The build pipeline (`src/start/build.ts`)

- **No `vscode` import** — confirmed by reading the whole file and by `grep -n vscode
  src/start/build.ts` (only doc-comment mentions). Every VS Code effect is threaded in via
  `BuildContext`/`ResumeContext`, wired only in `src/extension.ts`.
- **Marker lifecycle.** Written via temp-file + rename (`writeMarker`, lines 150-155) —
  genuinely atomic. `runPhase` (line 200) writes the marker *before* that phase's work runs;
  `test/unit/build.test.ts`'s "durably updated at each phase" test proves this by re-reading
  the marker from disk independently inside the `onPhase` hook, not from in-memory state.
  Deleted on success (line 279) and on an explicit cancel (`cancel()`, line 291-294, tested).
  **Deliberately left on a thrown failure** (digest/buildPlan/interpretPlan/write all throwing)
  — documented rationale (a transient failure might be worth retrying; digest/buildPlan are
  free to redo, interpretPlan is cache-keyed), tested directly
  ("a marker survives a THROWN digest error", line 361-373). This is a reasoned, disclosed
  design choice, not an oversight.
- **`cachedOnly` honoured** — threaded straight through to `interpretPlan`'s options
  (line 260).
- **GitHub clone.** Full clone, no `--depth` (`resolveRepo`, line 102, confirmed by
  `resolveRepo`'s test asserting `!args.includes('--depth')`), into
  `<globalStorageRoot>/refs/<owner>-<name>` (line 95), reused via `git fetch --all --tags` on a
  second call rather than re-cloning (line 96-98, tested).
- **Language refusal** — see Angle 1/AC3.
- **plan.json / request.json are NOT written atomically — gating.** `writeMarker` (same file,
  lines 150-155) explicitly uses a temp file + `fs.renameSync` specifically to avoid "a
  half-written marker" (its own comment). The actual deliverable, though
  (`buildFromRequest`, lines 272-274):
  ```
  fs.writeFileSync(path.join(buildDir(target), 'plan.json'), JSON.stringify(interpreted.plan, null, 2));
  fs.writeFileSync(path.join(buildDir(target), 'request.json'), JSON.stringify(request, null, 2));
  ```
  uses plain `writeFileSync`, no temp+rename. A crash exactly here leaves the marker behind
  (it's only deleted after these two writes succeed), so a *Resume* self-heals by re-writing
  the file in full — but a user who *Discards*, or simply never reloads within the 6-hour
  window, can be left with a truncated, invalid-JSON `plan.json` and no marker pointing at it,
  for T-4 (the walk) to trip over later with no explanation. No test exercises a crash between
  these two lines (understandably hard to simulate), so this is unexercised in either
  direction.

## Angle 4 — Skins

- **Independently censused repo-tour's `assets/skins/base.css`** myself (not trusting the
  test): `grep -oE -- '--[a-zA-Z][a-zA-Z0-9-]*\s*:' assets/skins/base.css | sed 's/[[:space:]]*:$//' | sort -u`
  → exactly 31 custom properties declared. `src/skins.ts`'s `BRIDGE_TOKEN_MAP` has exactly
  those same 31 keys — a set-equality check I did by hand, matching what
  `test/unit/skins.test.ts`'s own census test also asserts. (Aside, not a T-3 finding: base.css
  also *uses* `var(--warn)` at line 253 with no declaration anywhere in the file — a pre-existing
  repo-tour gap outside this ticket's scope and outside `bridgeCss()`'s stated contract, which
  is scoped to tokens base.css *declares*.)
- **Scoping.** `bridgeCss()` emits exactly one block, `:root:not([data-theme])`
  (`src/skins.ts:126-131`) — confirmed it is the only selector the function produces, so a real
  skin's `:root[data-theme="x"]` always wins on specificity, never fought.
- **The picker uses the real `SKINS` array, not a copy.** Traced the reference all the way
  through: `extension.ts`'s `runStartCommand` reads `core.loaded['skins']` (the dynamically
  `import()`-ed real module) and passes it into `StartPanel` → `renderStartHtml` →
  `pickerHtml(skins.SKINS, ...)`. `test/unit/skins.test.ts`'s AC6 suite proves this at runtime
  by `.push()`-ing a row onto the real module's `SKINS` array and observing it appear.
- **AC6 fixture test is real, not stubbed.** Read it in full: it copies repo-tour's real
  `assets/` tree to a temp dir, drops a `fixture.css` file in, calls the real
  `assets.configureAssets({ assetsDir: tmpAssets })`, pushes a fixture row onto the real
  `SKINS` array, and then asserts the fixture's marker CSS and the new `<option>` both appear
  in **`renderStartHtml`'s actual output** (not just `baseCss()`/`pickerHtml()` called
  directly) — including `<html lang="en" data-theme="fixture">`. Cleans up both the array
  mutation and the temp dir in a `finally`. This is exactly "drop a file + a row, zero
  extension change," proven through the real page.
- **`data-theme` stamped host-side before first paint.** `renderPage`'s `themeAttr` is computed
  synchronously in the string template (`src/webview/page.ts:69,74`) — there is no client-side
  step that could race or flash. `'system'` (repo-tour's own `DEFAULT_SKIN`) correctly omits
  the attribute so the bridge sheet decides instead.

## Angle 5 — State + restore

- `readSkin`/`writeSkin` on `context.globalState`, key `buildTutorials.skin`
  (`src/skins.ts:44-53`), wired in `panel.ts:41,55`. `context.workspaceState` remembers the
  build target (`CURRENT_BUILD_KEY`, `extension.ts:49,103,138`).
- The panel re-renders from host state on `onDidChangeViewState`'s `visible` transition
  (`src/start/panel.ts:65-67`), never from the view — matches spec §5.1's "the host re-renders,
  never from the view" doctrine exactly, and is why the start screen doesn't strictly need its
  own client-side `getState` restore: the server-rendered HTML already bakes in `this.state`'s
  values on every re-render.
- **Minor/advisory:** `page.ts`'s `restore()` helper (line 62) is defined but never called
  anywhere in `src/start/view.ts`'s client script — only `save()` is (line 144), so the
  `getState` half of the `getState`/`setState` contract is currently write-only for this page.
  Consistent with the host-authoritative design above (not a functional gap against any AC),
  but worth knowing it's unexercised.
- `resumeIfMarked` honours the 6-hour rule (`STALE_AFTER_MS`, `build.ts:310`, tested both just
  inside and outside the boundary) and offers Resume/Discard via a real
  `vscode.window.showInformationMessage(message, 'Resume', 'Discard')`
  (`extension.ts:149`) — **but see gating finding 1**: the wiring that's supposed to trigger
  this on a real activation has a real bug, and this specific integration (real `activate()` →
  `resumeOnActivate` → prompt) has no test at all, unit or vscode-test.

## Angle 6 — Core loading

- `'build'` added to `CORE_MODULE_NAMES` (`src/core.ts:32`). `doctor.ts`'s module count is
  computed dynamically off `CORE_MODULE_NAMES.length` (`formatReport`, `doctor.ts:131`), so no
  separate doctor.ts edit was actually needed for "8 expected" — confirmed by running the
  doctor myself (Angle 7): 8/8 modules ok, `build` module lists `buildPlan`, `interpretPlan`,
  `check`, `stubFile` among its exports.
- T-2's test was **tightened, not loosened**: `test/extension.test.ts`'s pre-existing `>= 6 ok
  modules` assertion is untouched; three new, stricter assertions were added on top (exact
  count `8`, the `build` module specifically `ok`, its detail naming `buildPlan`/
  `interpretPlan`).
- The core stays `external` in every esbuild entry point that could reach it
  (`grep -n external esbuild.mjs`: `vscode`/`repo-tour` on the extension, doctor, and both
  vscode-test bundles; `repo-tour` alone on the plain-mocha unit bundle).
- `grep -rn "require(.repo-tour" src test scripts` — zero hits, anywhere.

## Angle 7 — Ran it myself

```
$ npm run build            # esbuild.mjs — 5 entry-point groups, all clean
$ npm run typecheck        # tsc --noEmit — clean, no output, exit 0
$ npm run lint             # eslint . — clean, no output, exit 0
$ npm run test:unit        # plain mocha, dist/test/unit/**/*.test.js
    74 passing (436ms)     # matches the handoff's claimed 74 exactly
$ DISPLAY=:1 node scripts/run-tests.mjs        # real vscode-test, real DISPLAY
    13 passing (430ms)
$ env -u DISPLAY npm test  # rebuild + test:unit (74 passing) + xvfb-run -a vscode-test
    74 passing (97ms)
    13 passing (393ms)     # same 87 total, same result, under xvfb
$ ./build-tutorials doctor
    core found at .../repo-tour, v0.1.0 — loaded via its published subpath exports
    modules (8 expected):
      skins: ok — 6 exports ...
      build: ok — 6 exports: NULL_WITNESS, buildPlan, check, firstCommits, interpretPlan, stubFile
      ... (8/8 ok)
    grammars: ok — tree-sitter initialised; 4 grammars loaded
    claude: found at /home/corgea/.local/bin/claude
```
`git status --porcelain` was empty before and after every run above — no stray artifacts, no
accidental edits. Not run, per instructions (GUI): `./build-tutorials dev`, `start.sh`.

Adversarial probes (Angle 2), bundled from the real source with the project's own esbuild, run
with plain `node`: the XSS/escaping check and the malformed-message fuzz described above.

## Angle 8 — DO-NOT-TOUCH + hygiene

- **Ledger.** `git diff main...HEAD --stat -- .autodev` shows only `.autodev/handoffs/T-3.md`.
  A naive `git log main...HEAD -- .autodev/events.jsonl .autodev/tickets/` initially looked
  concerning (it lists a commit touching both), but `git merge-base --is-ancestor 25a8d2e
  feature/T-3-start-screen` says no — that commit (`T-4: refined, located, planned — holds for
  T-3`) is **main-only**, main advancing independently while this ticket was in flight, not
  something this branch did. Confirmed the two commits this branch *does* have that touch
  `.autodev/` (`3f9c613`, `2250aa8`) touch only `.autodev/handoffs/T-3.md`, exactly the
  deliverable the locate file calls for.
- **repo-tour untouched.** `git status` inside `/home/corgea/Desktop/Coding
  Projects/repo-tour` shows only `.autodev/conformance-history.jsonl` modified — its mtime
  (20:18:29) is ~90 minutes before this review session started and its content is an
  obviously-periodic background monitor snapshot (successive entries ~15 min apart); not
  caused by this ticket or by this review.
- **No secrets** in the diff (`grep -niE "api[_-]?key|secret|password|token\s*[:=]"` — no
  hits beyond code identifiers).
- **SPDX headers** present on every new `src/*.ts` file (checked all of them, not sampled).
- **`.gitignore`** already covers `.scratch/` (pre-existing; no change needed or made).
- No build artifacts (`dist/`, `.vscode-test/`, `node_modules/`) anywhere in the branch's
  commit history (`git log --name-only main...HEAD | grep -E '^(dist/|node_modules/|\.vscode-test/)'`
  — empty).

## Angle 9 — Hallucination check

- **Test counts, doctor output, module exports** — every specific number in the handoff
  (74 unit + 13 vscode-test = 87, both under `DISPLAY=:1` and `xvfb-run -a`; 8/8 modules;
  `build`'s export list) reproduced exactly by my own runs (Angle 7).
- **The "resume drill" is honestly disclosed, not overclaimed.** The handoff is explicit that
  AC4's literal "kill vscode-test mid-build" check was *not* done; it was simulated via a
  hand-written marker + a direct `resumeIfMarked` call. I read `test/unit/build.test.ts`'s
  `resumeIfMarked` suite and confirmed this is exactly what it does — no overclaim.
  **Waiver, accepted:** given the ticket's own "tests never spend tokens" constraint and its
  explicit deferral of the one real, model-calling run to the orchestrator's acceptance pass on
  sql-gauntlet (`.autodev/specs/T-3-start-screen.md`'s refinement notes), this substitution is
  reasonable and openly flagged (handoff judgment call #2), not hidden.
- **The visual-check/screenshot claim — independently corroborated via VSCodium's own logs**,
  a source outside the builder's control. `~/.config/VSCodium/logs/20260904T205402/window2/exthost/exthost.log`
  records `ExtensionService#_doActivateExtension bma-corgea.build-tutorials, startup: false,
  activationEvent: 'onStartupFinished'` at `21:29:47.528` — this ticket's own extension really
  activated in a real VSCodium window. `main.log` records, at `21:32:22.195-231`, that same
  extension host process (pid `1515085`) being killed (code 15/SIGTERM) and VSCodium logging it
  as `crashed with code 15 and reason 'killed'` — matching the handoff's specific claim, word
  for word, that "the window's extension-host process was killed directly rather than closed
  via the UI, which VSCodium reported as an unexpected termination." A separate,
  non-builder-authored file in the main checkout (`.autodev/reports/T-3-uat.md`, written by a
  different session at UAT) independently references the same screenshot and the same stray
  crash-recovery dialog. I could not locate the PNG file itself (likely cleaned up — the
  handoff never claims it was preserved), but the process-level evidence is specific, matches
  exactly, and was not something I could have predicted in advance. Not a hallucination.
- **The AC2 "Verbatim" claim does not fully hold — see gating finding below.** This is the one
  place a specific claim in the handoff ("Verbatim, under the idea field") does not survive a
  byte-level check.

## Summary of findings

**Gating:**

1. **`resumeOnActivate` never clears its own bookmark; the resume-on-activate wiring is
   untested.** `src/extension.ts:103` sets `context.workspaceState`'s
   `buildTutorials.currentBuild` at the start of every build and it is never cleared anywhere
   (`grep -rn CURRENT_BUILD_KEY src/` shows only the one set + one read). `resumeOnActivate`
   (`src/extension.ts:137-155`) does not check whether a marker exists before committing to
   `loadCore(...)` and opening a `withProgress` "Build Tutorials" notification — it only checks
   this *inside* `resumeIfMarked`, after that cost is already paid. Net effect: after the first
   build ever completes in a workspace (success, refusal, or cancel — all leave the bookmark
   set), **every future window activation** reloads the whole repo-tour core and flashes an
   empty progress notification, forever, for no reason. No test — unit or vscode-test —
   exercises `resumeOnActivate` or this integration at all; only the lower-level,
   VS-Code-free `resumeIfMarked` is tested, which correctly returns fast but only after
   `extension.ts` has already paid the unnecessary cost. Confirmed by direct code reading and
   grep, not by spinning up a second real activation cycle (VS Code's `Extension.activate()` is
   idempotent within one process, and reproducing a genuine second startup without exporting
   internal functions I should not add would itself be a code change).
2. **`plan.json`/`request.json` are not written atomically**, unlike the marker two functions
   away in the same file. `src/start/build.ts:272-274` vs. the temp+rename pattern at
   `src/start/build.ts:150-155`. A crash mid-write can leave a corrupt `plan.json` that a
   *Discard* or a >6h-stale marker leaves behind with nothing to signal it's broken.
3. **The message handler does not validate incoming message shapes.** Confirmed by directly
   feeding the real, compiled `handleStartMessage` (`src/start/protocol.ts:58-89`) malformed
   payloads: a non-string `repo` field throws `TypeError: repo.trim is not a function`; an
   unrecognized `type` throws from the `default` branch; a null/missing `payload` throws.
   `src/start/panel.ts:64` calls this via `void this.onMessage(message)`, so any of the above
   becomes an unhandled promise rejection. Exploitability is unconfirmed and likely low — the
   escaping (Angle 2) is solid, the CSP is nonce-only with no `unsafe-inline`, and
   `pick:repo`/`pick:target` never trust webview-supplied paths in the first place — but the
   review's own rubric names "an unvalidated message" as a gating category, and this is a real,
   reproduced gap, not a theoretical one.

**Advisory:**

1. **AC2's honesty line is not byte-exact against this ticket's own spec text.**
   `.autodev/specs/T-3-start-screen.md:12` (the ticket's own AC2, "captured, not re-composed")
   reads `tick recreate to build from a repo` (no inner quotes, no trailing period). The
   implementation (`src/start/request.ts:53`) reads `tick 'recreate' to build from a repo`
   (inner quotes present) — matching the earlier `.autodev/specs/T-1-build-tutorials-v1.md:203-204`
   wording instead (which itself also has a trailing period the implementation lacks). The two
   spec documents disagree with each other, so this isn't cleanly the builder's fault, but the
   handoff's explicit "Verbatim" claim doesn't fully hold against the ticket's own text, and
   `test/unit/request.test.ts:42` hardcodes the same (quoted) string rather than deriving it
   from either spec file, so no existing test could have caught the drift. Trivial to reconcile
   either way; worth a PM decision on which source is canonical.
2. `page.ts`'s `restore()` helper is defined but never called in `src/start/view.ts` — `save()`
   only, write-only for this page (Angle 5). Not a functional gap; the host-authoritative
   re-render design makes it genuinely optional here.
3. `assets/skins/base.css` references `var(--warn)` with no declaration anywhere in the file —
   a pre-existing repo-tour gap, outside `bridgeCss()`'s stated contract (tokens base.css
   *declares*) and outside this ticket's scope; noted for completeness only (Angle 4).

**Waivers:**
- AC4's literal "kill vscode-test mid-build" check was replaced with a disclosed simulation
  (marker durability read from disk independently per phase + a direct `resumeIfMarked` call
  against a hand-written "crashed" marker). Accepted given the ticket's explicit
  tests-never-spend-tokens constraint and its deferral of the one live/model-calling run to the
  orchestrator's acceptance pass on sql-gauntlet. This is unrelated to gating finding 1 above,
  which is about the untested *wiring* around `resumeIfMarked`, not the resume logic itself.

No DO-NOT-TOUCH violations, no secrets, no scope creep (nothing from T-4 onward appears in the
diff), no loosened tests. Recommend fixing gating findings 1-3 (all small, localized changes)
before merge; finding 1 in particular should not ship as-is since it's a real, permanent,
user-visible regression on every workspace that has ever run a build.

## Attempt 2

Verdict: PASS

**Reviewer:** fresh session (new context, no memory of attempt 1's session), auto-review stage,
attempt 2. **Branch:** `feature/T-3-start-screen` @ `391a3af` (handoff + regenerated
`kb/CODE-MAP.md`; merge-base with `main` still `d317672`). **Method:** read attempt 1's review
in full, the handoff's "Rework (attempt 2)" section, `.autodev/conventions.md` (present on
`main`, added by `main`-only commit `0fa48c7` after this branch was cut, so absent from the
worktree — read from `main`, nothing written there), then `git show` on all five rework commits
end to end. For each of the three gating findings I re-derived the claim independently rather
than re-reading the coder's tests and trusting them: bundled the real, current
`src/start/protocol.ts` and `src/start/view.ts` with the project's own esbuild and ran the
reviewer's original attack probes directly against the compiled functions myself (not the
coder's harness); wrote a byte-comparison script for the AC2 text rather than eyeballing it;
regenerated `kb/CODE-MAP.md` with the plugin's own deterministic generator to confirm it's
byte-identical to what's committed; recounted the skin-token census by hand from repo-tour's
live `base.css`; and diffed every touched test file for removed lines, not just added ones. Ran
the full suite myself twice (`DISPLAY=:1` and `env -u DISPLAY` under `xvfb-run -a`) and the
doctor once. One experiment — a temporary assertion added to an existing test in
`test/unit/build.test.ts` to check for leftover `.tmp` files after a real successful build —
was reverted with `git checkout --` immediately after use; a second experiment (a literal
source-level revert-check on `src/start/panel.ts`, mirroring the coder's own disclosed revert
checks) was blocked by this environment's own edit-classifier before it touched the file, so I
substituted a non-destructive equivalent (below, Finding 3) that needed no source edit at all.
`git status --porcelain` was empty before, during (between reverted experiments), and after this
session. Nothing pushed, merged, or run through `tracker.mjs`; `dev`/`start.sh` not run.

### Finding 1 — resume-on-activate leak — CLOSED

Read the full diff (`git show 0306aa0`) and the resulting `src/extension.ts`/`src/start/build.ts`
in place. The fix is structurally sound: `resumeOnActivate` now calls the new
`readFreshMarker(target)` (`src/start/build.ts:203-208`, one `fs.readFileSync` + the same 6-hour
`STALE_AFTER_MS` rule `resumeIfMarked` already used, now shared) and returns — clearing the
bookmark first — **before** touching `deps.loadCore` or `deps.withProgress` at all. The old code
(visible in the diff's `-` lines) called `loadCore` and opened `withProgress` unconditionally
whenever `target` was set, discovering there was nothing to do only *inside* `resumeIfMarked`,
after both costs were paid — the diff by itself is a complete, byte-for-byte record of the exact
bug attempt 1 described; no speculation needed to confirm what it did.

Ran `test/extension.test.ts`'s five new vscode-test cases myself (all pass, see below) and read
them line by line: they are **counted calls through an injected seam**, not string assertions.
`CountingDeps` (`test/extension.test.ts:143-163`) increments `loadCoreCalls`/`progressCalls` and
records every `confirm()` message; the no-marker, stale-marker, and no-target cases all assert
`loadCoreCalls === 0` and `progressCalls === 0` against a real `ExtensionContext`'s real
`workspaceState` (not fakeable outside an extension host, which is why this has to be a
vscode-test) — the one place a string is matched is a secondary `assert.match` on the confirm
prompt's wording in the fresh-marker case, additive to, not a substitute for, the call-count
assertions. The fresh-marker case discriminates against the other three (proves the harness
isn't just trivially returning 0 for everything), and Discard clears both the on-disk marker and
the workspaceState key. I verified `forgetUnlessResumable` and `resumeIfMarked`'s dismiss/Discard
branches by reading `src/start/build.ts:374-388` directly: a dismissed `confirm()` returns
`undefined` from `resumeIfMarked` without calling `deleteMarker`, so the marker (and hence the
key) survives — exactly what the "fresh marker" test asserts.

One thing the five new tests do **not** directly cover: `runBuild` (the *normal*, non-resume
build path, `src/extension.ts:117-127`) also now calls `forgetUnlessResumable` after a build
finishes, and nothing drives that specific call site end-to-end through a real
`buildTutorials.start` command + webview submit + `workspaceState` read-back. I did not add a
test for it (out of scope — "do not fix code" — and it would need either exporting a currently-
private function or scripting a full command-plus-webview round trip). I'm satisfied by
composition instead: `forgetUnlessResumable` is the exact same function object exercised by the
Discard case in the `resumeOnActivate` suite (so its own logic — clear the key iff no marker is
on disk — is proven), and `buildFromRequest`'s marker deletion on success/refusal/cancel is
unchanged, pre-existing, and still independently tested at `test/unit/build.test.ts:271`
("the marker must be gone on success"), `:304` (deterministic refusal), and `:359` (explicit
cancel) — all three still pass. Composing two independently-tested facts is weaker than one
dedicated end-to-end test, but the composition is trivial (one `if` on a value one of those two
facts already produces), so I'm not gating on it — flagging it for the record rather than
asserting more coverage than exists.

### Finding 2 — non-atomic plan/request writes — CLOSED

Read `writeJsonAtomic` (`src/start/build.ts:177-182`) and confirmed all three durable writes
(marker, `request.json`, `plan.json`) now route through it, in the order the commit message
claims: `request.json` then `plan.json` then the marker is deleted
(`src/start/build.ts:318-327`, `buildFromRequest`). Ran the four new tests in
`test/unit/build.test.ts` myself and read them:
- `writeJsonAtomic` with an injected `renameSync` that throws leaves **no file at the
  destination** and the complete JSON sitting in `<file>.tmp` — this is the assertion that a
  plain `writeFileSync` (what this replaced) would fail, since it writes the destination
  directly.
- The same call uninterrupted lands complete, temp file gone.
- A full, real `buildFromRequest` run (real repo-tour core, `cachedOnly: true`, a real TS
  fixture) with `renameSync` rigged to fail only when renaming onto `plan.json`: `outcome.ok`
  is `false` with that reason, `plan.json` does not exist at all, and the marker is still on
  disk at phase `write`, still fresh enough to be offered for resume — i.e., exactly the
  "either a marker or a complete plan, never neither" invariant the commit claims.
- The rename order really is `['request.json', 'plan.json']`, the marker itself is renamed
  into place independently (≥4 times, once per phase, via the same helper), and it's removed
  only once both deliverables exist.

I independently checked the one thing not explicitly asserted anywhere: that a **successful**
build leaves no `.tmp` file behind. Temporarily added one assertion
(`fs.readdirSync(buildDir(target)).filter(f => f.endsWith('.tmp'))` deep-equals `[]`) to the
existing "plan.json + request.json are written and plan.json validates against the real schema"
test, rebuilt, ran it — passed — then reverted the test file with `git checkout --` and rebuilt
again; `git status --porcelain` was empty before and after. No `.tmp` files survive a real
successful build.

### Finding 3 — unvalidated inbound messages — CLOSED

Bundled the real, current `src/start/protocol.ts` standalone with the project's own esbuild
(no stub) and ran the reviewer's three original probes against the compiled
`parseInboundMessage` myself, plus the variants the new tests add:

```
probe1 (non-string repo field)      -> null
probe2 (null payload.request)       -> null
probe2b (missing payload entirely)  -> null
probe3 (unknown type)               -> null
```

All four return `null`, no throw. I also independently probed properties the checklist calls
out specifically:
- **Prototype-pollution-style extra key dropped:** fed a request payload with an extra
  `extraJunk` field and a literal `__proto__: { polluted: true }` key; the parsed object
  contains exactly the five known fields, `hasOwnProperty('extraJunk')` is `false`,
  `hasOwnProperty('polluted')` is `false`, and `({}).polluted` is `undefined` afterward — no
  global prototype pollution either.
- **`dial` validated against `DIALS`:** an invalid dial string returns `null`.
- **A valid message works end to end:** parses to the expected object and
  `handleStartMessage` accepts it, replying `form:problems` with the real (empty-repo)
  validation problem — the pipeline downstream of the parser is untouched and still works.

To confirm the guard is load-bearing rather than decorative, I wanted to literally revert the
fix in `src/start/panel.ts` and re-run the malformed-message test, the way the coder's own
disclosed revert-check did. That edit was blocked by this environment's edit-classifier before
it touched the file (a source-file edit, even temporary and clearly reverted afterward, was
refused) — correctly, since it's not this seat's job to modify code, so I did not try to route
around it. Instead I got equivalent evidence without touching any file: called the real,
*unvalidated* `handleStartMessage` directly (the pure function `parseInboundMessage` sits in
front of, unchanged by this rework) with the reviewer's own probes. Probe 1 rejects with
`TypeError: repo.trim is not a function` — attempt 1's exact error, reproduced byte for byte —
and probe 3 throws synchronously from the exhaustive `default` branch, which, called the way the
real listener calls it (`void this.onMessage(...)`), is precisely how attempt 1 described it
becoming an unhandled promise rejection (it crashed my own probe script the same way). This
proves the crash the fix guards against still exists one layer down, unchanged — so the guard in
front of it is doing real work, not standing in front of an already-safe function.

Read (not just ran) `test/start.test.ts`'s two new vscode-test cases: six probes through the real
`StartPanel.dispatchForTest` (the exact path `onDidReceiveMessage` calls) each resolve
`undefined`, panel state is provably untouched (`getStateForTest()` still equals
`emptyRequest()`/`{}`), and exactly one log line is emitted for all six — the "logs once" rule
checked literally, not just claimed — while a well-formed message dispatched the identical way
still round-trips through `onSubmit` and posts the expected reply, and is *not* logged. This
discriminates: the six ignored probes and the one working message go through the same code path
with different, correct outcomes.

Additional adversarial shapes beyond the checklist (bonus, not gating): a circular-reference
payload, a `Symbol` value where a string was expected, and a bare array/function as the whole
message all return `null` cleanly. A live object with a throwing getter for `payload` does throw
on property access — but `vscode.Webview.postMessage` structurally clones its argument, so a
getter could never survive that boundary in the first place; not a reachable shape from a real
webview, and attempt 1 didn't flag it either.

### Advisory 1 — AC2 text byte-exact — CLOSED

Did not eyeball it. Wrote a script that extracts AC2's quoted string from
`.autodev/specs/T-3-start-screen.md` by regex and `IDEA_FIRST_MESSAGE`'s value from
`src/start/request.ts:57` by regex, and compared them as strings:

```
spec AC2 text  : "Idea-first builds arrive with T-9 — tick recreate to build from a repo"
implementation : "Idea-first builds arrive with T-9 — tick recreate to build from a repo"
byte length spec: 72  impl: 72
BYTE-EXACT MATCH: true
spec dash codepoint: 2014   impl dash codepoint: 2014
```

Both the em dash (U+2014, not a hyphen or en dash) and every other byte match exactly.
`test/unit/request.test.ts:42-47`'s hardcoded copy was independently confirmed to contain this
same string (retyped from the spec, not derived from the constant it checks, per the commit
message — appropriately, since deriving it would defeat the assertion's purpose). README.md's
quotation matches too, once its prose line-wrap is normalized (`Idea-first builds arrive with` /
`T-9 — tick recreate to build from a repo` is one sentence split across two source lines, not a
content difference) — confirmed with a whitespace-normalizing substring check, not by eye.

### Regressions — none found

Diffed every touched test file for **removed** lines specifically (not just added), across all
four code commits:
- `test/extension.test.ts`, `test/unit/protocol.test.ts`: only import-line reorganisation
  (a symbol added to an existing import statement, which git renders as one line removed + one
  added).
- `test/start.test.ts`: zero removed lines — pure addition at the end of the file.
- `test/unit/build.test.ts`: two removed lines, both the same import-reorganisation pattern.
- `test/unit/request.test.ts`: exactly one assertion changed — the AC2 hardcoded string, which
  changed because the string it checks changed (Advisory 1) — still a byte-exact `assert.equal`
  against a hardcoded value, not loosened to a substring/regex match.

No assertion was deleted, weakened, or turned into a no-op anywhere in the diff. All of attempt
1's previously-passing tests still exist and still pass (see Hygiene below for the full run).

Re-verified two of attempt 1's positives that the rework's diff doesn't touch, rather than just
trusting "the diff doesn't touch those files":
- **CSP + escaping.** `git diff` confirms `src/webview/`, `src/start/view.ts` are byte-identical
  to what attempt 1 reviewed. Re-bundled `src/start/view.ts` standalone anyway and re-ran the
  same adversarial render: `default-src 'none'; style-src 'nonce-…'; script-src 'nonce-…'`, no
  `unsafe-inline`; the `<img src=x onerror=alert(1)>` idea payload and the `"><script>` breakout
  in repo/target both come back fully escaped (`&lt;img…`, `&quot;&gt;&lt;script&gt;…`); two
  separate renders produce two different nonces.
- **Skin-token census.** Recounted `assets/skins/base.css`'s declared custom properties by hand
  (`grep -oE -- '--[a-zA-Z][a-zA-Z0-9-]*\s*:' … | sort -u`): **32**, not the 31 attempt 1
  reported. `BRIDGE_TOKEN_MAP` also has 32 keys, and a `diff` of the two sorted sets is empty —
  the exact-match property attempt 1 verified still holds completely; the discrepancy is an
  arithmetic slip in attempt 1's own count (or a one-token drift in repo-tour, a sibling repo
  outside this ticket's control, between the two review sessions), not a regression — `src/skins.ts`
  and `test/unit/skins.test.ts` are untouched by every rework commit (`git diff --stat` empty for
  both), and repo-tour's tree shows no changes of its own beyond an unrelated, pre-existing
  background-monitor file with a two-hour-old mtime. The test itself derives the census from the
  live CSS file rather than a hardcoded number, so this arithmetic slip was never load-bearing.
- AC6's fixture-skin test, the fake-DOM AC7 suite, and the schema-validated AC5 pipeline are
  likewise untouched by every rework commit and still pass (see full run below).

### Hygiene

- **No new dependency.** `git diff 2250aa8..HEAD -- package.json package-lock.json` is empty.
- **No DO-NOT-TOUCH violation.** Per `.autodev/locates/T-3.md`'s list (`src/doctor.ts` beyond
  the module count, `scripts/*`, `.github/workflows/ci.yml`, repo-tour's tree, `.autodev/**`
  ledger): `git diff --stat c445b2e..HEAD` touches exactly `.autodev/handoffs/T-3.md`,
  `README.md`, `kb/CODE-MAP.md`, `src/extension.ts`, `src/start/{build,panel,protocol,request}.ts`,
  and five test files — none of the DO-NOT-TOUCH paths, and `.autodev/handoffs/T-3.md` is the
  ticket's own deliverable, not the ledger (`events.jsonl`/`tickets/`/`releases/`, absent from
  the diff).
- **`kb/CODE-MAP.md`.** Regenerated it myself with the plugin's own deterministic generator
  (`node .../autodev-plugin/scripts/code-map.mjs .`): reported "code map unchanged", i.e.
  byte-identical to what's committed. The committed diff itself is exactly three export-list
  lines (`resumeOnActivate` on `extension.ts`; `readFreshMarker`/`realFsWriteOps`/
  `writeJsonAtomic` on `build.ts`; `parseInboundMessage` on `protocol.ts`) — matches every new
  exported symbol actually introduced, nothing else.
- **No secrets.** `grep -niE "api[_-]?key|secret|password|token\s*[:=]"` over the full rework
  diff — the one hit is `token: vscode.CancellationToken`, a parameter name, not a credential.
- **SPDX headers** present on every touched `src/*.ts` file.
- **No suppressions.** No `eslint-disable`, `@ts-ignore`, `@ts-expect-error`, or `as any` /
  `: any` anywhere in the rework diff — lint and typecheck pass honestly, not by silencing.
- **No config loosened.** `tsconfig.json`, `eslint.config.mjs`, `.vscode-test.mjs`,
  `esbuild.mjs`, `package.json` are all byte-identical to the attempt-1-reviewed commit.
- **No build artifacts committed** (`git log --name-only c445b2e..HEAD | grep -E
  '^(dist/|node_modules/|\.vscode-test/)'` — empty).
- **Commit metadata.** All five rework commits: author `Corgea <blackmapartistry@gmail.com>`,
  both trailers (`Co-Authored-By: Claude Fable 5.1`, `Claude-Session: …`) present — matches
  `.autodev/conventions.md`.
- **Not pushed.** `feature/T-3-start-screen` has no upstream tracking configured; `main` (read
  only, for `conventions.md`) is untouched by this session.

### Commands run, real output

```
$ npm run typecheck
tsc -p tsconfig.json --noEmit          # clean, exit 0

$ npm run lint
eslint .                                # clean, exit 0

$ DISPLAY=:1 npm test
  89 passing (unit, plain mocha)
  20 passing (vscode-test, real DISPLAY)
                                         # 109 total — matches the handoff's claimed 89+20 exactly

$ env -u DISPLAY npm test                # xvfb-run -a under the hood
  89 passing
  20 passing
Exit code: 0                            # same 109, same result, under xvfb

$ ./build-tutorials doctor
core found at .../repo-tour, v0.1.0
modules (8 expected): skins, extract, llm, digest, interpret, ask, types, build — all ok
grammars: ok — tree-sitter initialised; 4 grammars loaded
claude: found at /home/corgea/.local/bin/claude

$ git status --porcelain   # empty before, between, and after every run above
```

Not run, per instructions: `./build-tutorials dev`, `start.sh`, `tracker.mjs`; nothing pushed or
merged.

### Verdict rationale

All three gating findings from attempt 1 are genuinely closed, each verified independently
against the real compiled code (not just by reading or re-running the coder's own tests), each
with a test that demonstrably discriminates (counts real calls, or reproduces the exact original
crash when the guard is bypassed). The one advisory (AC2 text) is byte-exact, confirmed by
script rather than by eye. No regression: every previously-passing test still exists and still
passes, no assertion was weakened, and the two most security-relevant attempt-1 positives (CSP/
escaping, the skin-token census) were re-derived independently rather than assumed from an
untouched diff. Hygiene is clean: no new dependency, no DO-NOT-TOUCH violation, `kb/CODE-MAP.md`
regenerates identically, no secrets, no suppressions, correct commit metadata. The one honestly
disclosed gap — `runBuild`'s normal-flow key-clearing has no dedicated end-to-end test, only a
composition of two independently-tested facts — is low-risk (a one-line, trivial composition)
and not gating. PASS.
