Verdict: PASS

# Review — T-2, Extension skeleton, core attach, doctor

**Reviewer:** fresh session, auto-review stage. **Branch:** `feature/T-2-skeleton` @ `4307b8e7966d7bfda388eec2a5cf5ab12147cacd`
(base `main`). **Method:** read spec/locate/plan/handoff and T-1 §3/§5.1/§10, read the full
`git diff main...HEAD` (skipped `package-lock.json` beyond a glance), then independently
re-ran every check rather than trusting the handoff's account, plus adversarial probes the
handoff didn't do (environment tampering, a synthetic Node self-reference test, an injected
lint error, a live re-check mid-review after a dependency changed underneath the ticket — see
Angle 2). No code was fixed; nothing was pushed, merged, or run through `tracker.mjs`.

## Headline

All 8 ACs (5 base + 3 addendum) hold up under independent, hands-on re-verification, not just
document review. One nuance was found in `scripts/doctor-cli.mjs`'s default-argument handling
(Angle 2/4) that looked like a real bug on first read; traced to its root cause, it turns out
to be unobservable in any setup npm can actually produce, so it's recorded as an advisory
comment/precision nit rather than gating — reasoning below, not just asserted. Nothing else
rises even to that level. The handoff is unusually honest: every specific claim I checked
against reality (module counts, test names, exact doctor output, the `vsce package` error
message, symlink target, `start.sh`'s pause shape) matched, and it proactively surfaces its
own limits (CI not pushed, AC5's "matching repo-tour" caveat, a stale ledger snapshot) rather
than papering over them.

## Angle 1 — Spec conformance (8 ACs)

Checked each AC's named check for realness, not just presence.

- **AC1 (manifest).** `package.json`: id `build-tutorials` (publisher `bma-corgea`),
  `activationEvents` on both commands exactly matching `contributes.commands`,
  `buildTutorials.repoTourPath` default `../repo-tour`, `engines.vscode ^1.95.0`. Test
  "activates and registers its two commands" — reran it myself in a real extension host, passes.
- **AC2 (bundling, unbundled core).** Verified two ways beyond reading the source: (a) built
  `dist/extension.js` and grepped it — the extension's own code (e.g. `resolveCoreRoot`) is
  inlined, `require("repo-tour")` appears nowhere, no repo-tour source strings
  (`ASK_PERSONA`, `skinsDir`, `GRAMMAR_FOR`) leaked into the bundle, and the `import()` calls
  survive as genuine runtime dynamic imports rather than being statically resolved; (b) the
  "loads the core... and calls baseCss()" test passes for real against the sibling checkout.
- **AC3 (doctor).** Ran the doctor myself, both ways specified in the check: real sibling core
  (found, 7/7 modules ok, 4 grammars, claude found) and `repoTourPath` pointing at
  `/this/path/does/not/exist` (found:false, reason names the exact path, exit 1, no throw —
  see Angle 7 transcript). Also independently forced the "claude absent" branch (Angle 3) since
  claude happens to be installed here, so the doctor's happy path alone wouldn't prove it.
- **AC4 (CI).** See Angle 5 — not run on GitHub (nobody pushed), but every step was reproduced
  locally and the workflow was checked structurally for what would actually break a GitHub run.
- **AC5 (license).** `NOTICE`, SPDX headers, README Source link all present; independently read
  repo-tour's actual practice (`LICENSE.txt`, README "## Licence" section, no per-file headers,
  no NOTICE) and confirmed the handoff's characterization of it is accurate, not invented.
- **AC6 (`./build-tutorials`).** `dev`/`build`/`test`/`doctor`/`package` all present; ran build,
  test, doctor (both paths), package myself (Angle 7). Did not run `dev` (opens a GUI, out of
  bounds per instructions) — verified by reading: builds, resolves `codium` before `code`,
  prints which, `mkdir -p` on a quoted path for the default scratch folder, no `rm -rf`
  anywhere in the script (grepped for it — zero hits).
- **AC7 (`start.sh`).** Not run (opens a GUI). Diffed byte-for-byte against repo-tour's actual
  `start.sh`: identical structure, identical `[ -t 0 ] && [ -t 1 ]` gate, identical
  `BUILD_TUTORIALS_NO_PAUSE`/`REPO_TOUR_NO_PAUSE` naming pattern, identical "Press Enter to
  close this window…" wording. The one intentional difference (repo-tour's leaves a server
  running and says so; this one doesn't and says so) is accurate to what each script does, not
  a copy-paste miss.
- **AC8 (headless tests).** Ran `npm test` myself with `DISPLAY=:1` set (runs `npx vscode-test`
  directly) and with `DISPLAY` unset (`env -u DISPLAY npm test`, runs under `xvfb-run -a`).
  4/4 pass both ways. Transcripts under Angle 7.

`buildTutorials.start` really does show the honest arrives-with-T-3 message
(`src/extension.ts#runStartCommand`) rather than doing nothing — read directly, not inferred.

**No scope creep found**: nothing from T-3 onward (start screen, tree view, webviews,
`build/plan.ts`, skins bridge) appears anywhere in the diff.

## Angle 2 — The loader (`src/core.ts`)

- **Exports-then-dist, verified against a real transition, not just read.** While this review
  was in progress, repo-tour's own **T-11** (add the `exports` map — the exact cross-shop
  dependency `.autodev/locates/T-2.md` calls out: "`src/core.ts` must work before AND after")
  **merged to `main` live**, changing `/home/corgea/Desktop/Coding Projects/repo-tour` from no
  `exports` field to a full one mid-review (confirmed via its own `git log`: `af2a8b1` landed
  at `package.json` mtime `17:43:26`, after my first doctor run at `17:41`). I reran the doctor
  and the full test suite on both sides of that transition with **zero code changes**:
  - Before: `core found... loaded via dist paths (repo-tour has no exports map yet)`, 7/7 modules, 4/4 tests pass.
  - After: `core found... loaded via its published subpath exports`, same 7/7 modules, same 4/4 tests pass.
  This is about as strong a real-world confirmation of the "try exports, fall back to dist"
  contract as this ticket could possibly get, and it is not something the builder could have
  tested themselves (T-11 hadn't landed while they worked).
- Also independently verified the *mechanism*, not just the outcome: built a throwaway package
  in the scratchpad with `"name": "repo-tour"` and a real `exports` map, and confirmed
  `createRequire(path.join(root, 'package.json')).resolve('repo-tour/skins')` — exactly what
  `loadOneModule` does — genuinely resolves via Node's self-reference feature, scoped to
  whatever `root` is (i.e. it honors a custom `repoTourPath`, not just whatever happens to be
  linked into this extension's own `node_modules`).
- **`external: ['repo-tour']`** on every esbuild entry point (extension, doctor, test) —
  confirmed never bundled (Angle 2/AC2 above).
- **`resolveCoreRoot`** handles absolute (as-is), relative-to-extension
  (`path.resolve(extensionRoot, trimmed)`), and empty/cleared-setting
  (`require.resolve('repo-tour/package.json')`, i.e. ordinary node_modules resolution) — all
  three cases in the angle's checklist are present. A nonexistent path is a **report**
  (`found:false`, reason names the exact path), never a throw — reran this myself (Angle 7).
- **Path-with-spaces.** The actual worktree path has the exact space this angle warns about
  (`.../Coding Projects/VSCode-LLM-Tutorial-T-2`), and repo-tour's sibling checkout does too.
  Confirmed `pathToFileURL(...).href` is used for both dynamic `import()` calls in
  `loadOneModule` (not string concatenation) by reading the code, and then proved it matters in
  a standalone probe: built a scratch package under a directory literally named `space test
  dir`, imported its `dist/skins.js` via `pathToFileURL(...).href` (works, correctly
  percent-encodes the space) — the code under review does this correctly. (On this Node
  version a naive `'file://' + path` string also happened to succeed in my probe, so the
  concrete repo-tour bug this angle references may not reproduce with a single space on
  Node 22 specifically — but the code doesn't rely on that; it uses the correct API either way.)
  Separately, and more convincingly: the real vscode-test suite runs from inside this
  space-containing path against the real space-containing sibling repo, every time, and passes
  — that is the space-safety proof that actually matters here, and it is real, not synthetic.
- **Advisory (not gating) — `scripts/doctor-cli.mjs` default-argument nuance.** Its comment
  says the no-argument case uses "the same default the extension's `buildTutorials.repoTourPath`
  setting has" via `process.argv[2] ?? '../repo-tour'`. But `build-tutorials`'s `cmd_doctor`
  always calls `node scripts/doctor-cli.mjs "${1:-}"` — under `set -u`, an omitted `$1` becomes
  an explicit empty-string argument, not an absent one. So `process.argv[2]` is `''`, not
  `undefined`, `??` never fires, and `resolveCoreRoot` actually takes the *empty-setting*
  branch (node_modules resolution) rather than the *relative-path* branch
  (`path.resolve(extensionRoot, '../repo-tour')`) the comment describes. I verified this is a
  real code-path divergence (traced it with a bash instrumentation probe — `cmd_doctor`'s own
  `$1` is confirmed empty-string, length 0, not unset). I flagged this as a possible real bug
  and chased it further before deciding severity: under npm's own semantics, a `file:../repo-tour`
  dependency in `package.json` **always** makes `node_modules/repo-tour` resolve to exactly
  `../repo-tour` relative to the project root — there is no `npm install`/`npm ci` outcome
  where the two branches could disagree; only manually re-symlinking `node_modules/repo-tour`
  by hand to somewhere else, independent of `npm`, would make them diverge, and even then the
  CLI never reads VS Code's `settings.json` at all (by design — its own `[path]` argument is
  the only override), so "should match the user's custom VS Code setting" was never an
  achievable bar for the no-argument case in the first place. **Waiver:** recorded as advisory,
  not gating — it's a real inconsistency between a comment and which of two code paths runs,
  but not an inconsistency in *outcome* under any reachable configuration. Worth a one-line
  tidy (e.g. `process.argv[2] || '../repo-tour'`, or have the bash wrapper only pass `$1` when
  set) so the comment stops being misleading, but not worth a rework cycle by itself.

## Angle 3 — The doctor

- **No `vscode` import** in `src/doctor.ts` — confirmed by reading the whole file (only imports
  from `./core.js`); `scripts/doctor-cli.mjs` imports `dist/doctor.js` specifically (never
  `dist/extension.js`), so the CLI path structurally cannot reach a `vscode` import even
  indirectly. `./build-tutorials doctor` and the in-editor command are provably the same
  function (`runDoctor`), not a lookalike reimplementation.
- **Missing claude → reason, not a throw.** claude happens to be installed on this machine
  (`/home/corgea/.local/bin/claude`), so the happy-path doctor run alone doesn't exercise this.
  I forced the negative case myself: `env -i HOME=<fake, empty> PATH=/usr/bin:/bin node
  scripts/doctor-cli.mjs` — genuinely hides claude from both PATH and `resolveBin`'s known-path
  list. Result: `claude: not found — not found on PATH or in known install locations (set
  BUILD_TUTORIALS_CLAUDE to override)`, exit code **0** (claude is correctly treated as a soft
  dependency — it doesn't fail the doctor's exit status, only `core`/`modules`/`grammars` do).
  No throw, a real reason, core/modules/grammars all still reported `ok` around it.
- **Grammars: a real call, and the "confirms four" claim holds up, not just assumed.** Read
  repo-tour's actual `initParsers` (`src/extract.ts`): it calls `Parser.init()` once, then for
  each requested language loads and registers a real tree-sitter grammar; if a
  `Language.load()` call throws, the whole function throws (no partial-success swallowing), and
  an unrecognized language name is silently skipped rather than erroring. Since
  `checkGrammars` passes the literal four names (`python/javascript/typescript/tsx`), which
  exactly match `extract.ts`'s own `GRAMMAR_FOR` keys, "a clean resolve ⇒ all four loaded" is
  actually true today, not an assumption papering over a gap — though it is worth noting this
  guarantee is contingent on `EXPECTED_GRAMMARS` never drifting out of sync with repo-tour's
  own grammar keys (advisory: no test would catch that drift, since `initParsers` swallows
  unknown names rather than erroring on them).

## Angle 4 — Scripts

Read `build-tutorials` and `start.sh` fully, line by line.

- `dev`: prefers `codium`, falls back to `code`, prints which (`_ok "found codium (VSCodium) —
  using it"` / `... code (VS Code) ...`) — matches "this machine has codium and no code."
  Default scratch folder: `$ROOT/.scratch/$(date +%Y%m%d-%H%M%S)`, created via quoted
  `mkdir -p "$folder"`. An explicit folder argument is also `mkdir -p`'d, quoted, with a `_die`
  on failure. Did not execute (GUI).
- **No destructive `rm` anywhere.** Grepped `build-tutorials`, `start.sh`, and both
  `scripts/*.mjs` for `rm -rf`/`rm -f` — zero matches. There is no variable-could-be-empty
  deletion risk because there is no deletion at all in this diff.
- `start.sh` mirrors repo-tour's pause semantics essentially verbatim — see Angle 1/AC7. Not
  executed (GUI), but the logic was diffed against the real file it claims to mirror, not
  taken on faith.
- Both scripts default cleanly: `./build-tutorials help`, bare `./build-tutorials`, and
  `./build-tutorials bogus-command` all behave sanely (usage text, usage text, `unknown
  command: bogus-command (try: dev, build, test, doctor, package)` + exit 1) — ran all three.
- SPDX headers are on every new `.ts`/`.mjs`/`.yml` file but **not** on `build-tutorials` or
  `start.sh` (plain bash, no extension). The handoff's claim was precisely scoped to
  "`.mjs`/`.yml` config/script files," which is accurate and doesn't cover these two — not a
  hallucination, just an inconsistently-applied habit. Advisory, and moot in the same way AC5's
  broader divergence is: repo-tour itself carries zero per-file headers, so this doesn't fall
  short of the AC5 bar either way.

## Angle 5 — CI (`.github/workflows/ci.yml`)

Could not trigger a real GitHub run (pushing is out of bounds for both the builder and me), so
I did the next best thing: verified everything that determines whether it *would* go green.

- Two side-by-side checkouts (`path: VSCode-LLM-Tutorial`, and `repository: BMA-Corgea/repo-tour`
  at `path: repo-tour`) — confirmed **both `BMA-Corgea/repo-tour` and
  `BMA-Corgea/VSCode-LLM-Tutorial` are public** (`gh api repos/BMA-Corgea/repo-tour` →
  `"private": false`; same for the extension repo), so the cross-repo checkout needs no extra
  token/deploy-key and won't 404 under the default `GITHUB_TOKEN`.
  `package-lock.json`'s `node_modules/repo-tour` entry is `{"resolved": "../repo-tour", "link":
  true}` — a relative link, which is exactly what the two-sibling-checkout CI layout
  reproduces; `npm ci` in `VSCode-LLM-Tutorial` will correctly link to the `repo-tour` checkout
  placed next to it.
- Order is correct: checkout × 2 → setup-node@22 → build repo-tour (`npm ci && npm run build`,
  `working-directory: repo-tour`) → `npm ci`/typecheck/lint (`working-directory:
  VSCode-LLM-Tutorial`) → **install Xvfb before Test** (needed since `scripts/run-tests.mjs`
  wraps itself in `xvfb-run -a` whenever `DISPLAY` is unset, which it is on GitHub runners) →
  `npm test` → `npm run package` (`vsce package --no-dependencies`) → upload the `.vsix`. Every
  `working-directory` matches the checkout it should.
- Action versions: `actions/checkout@v4`, `actions/setup-node@v4`, `actions/upload-artifact@v4`
  — current, real, maintained major versions.
- YAML parses cleanly (`python3 -c "import yaml; yaml.safe_load(...)"`).
- The `vsce package --no-dependencies` fix and its documented reason (plain `vsce package`
  hard-errors trying to walk into the sibling repo-tour's own `node_modules`, producing
  archive-escaping relative paths) — reproduced myself in Angle 7, exact same shape of output.
- **Genuinely unverified, and correctly disclosed as such**: an actual green run on GitHub
  (AC4's literal check). Neither the builder nor I could push to test this. Everything else
  short of that has now been checked.

## Angle 6 — DO-NOT-TOUCH + hygiene

- `.autodev/events.jsonl`, `.autodev/tickets/`, `.autodev/releases/` — **not touched** by this
  diff (`git diff main...HEAD --stat -- .autodev/` shows only `.autodev/handoffs/T-2.md`,
  which the locate file explicitly allows). Confirmed with an explicit
  `git diff --name-only | grep -E '^\.autodev/(events|tickets|releases)'` — no output.
- `LICENSE` untouched (not in the diff at all).
- repo-tour's own tree was never edited by this ticket — confirmed `git status` inside
  `/home/corgea/Desktop/Coding Projects/repo-tour` is pristine except for
  `.autodev/conformance-history.jsonl`, which is a live cross-repo monitoring artifact a
  background AutoDev watcher updates continuously on its own timer (already mid-update before I
  started, and also touched in the unrelated main `VSCode-LLM-Tutorial` checkout's own working
  tree) — not something this ticket's diff or this review produced or should revert.
- `.gitignore` adds `.scratch/` and `.vscode-test/`; `*.vsix` was already ignored from the
  initial commit. Verified `npm run package`'s output `.vsix` and every `dist/`/`node_modules/`
  byproduct of my own test runs stayed untracked (`git status` after all testing shows only the
  pre-existing, unrelated `conformance-history.jsonl` line).
- Secret scan (`grep -niE "api[_-]?key|secret|password|token|BEGIN (RSA|OPENSSH|PRIVATE)|AKIA[0-9A-Z]{16}"`
  over the whole diff) — no matches.
- License headers: present on every new `.ts`/`.mjs`/`.yml`/workflow file (verified by grepping
  each one individually, not sampling); absent on the two plain-bash scripts (advisory, see
  Angle 4). `NOTICE` present and substantive; README carries a Source link.
- Minor advisory: `.vscodeignore` excludes `test/**` (TS sources) but not `dist/test/**` — the
  packaged `.vsix` (rebuilt and inspected myself) ships `dist/test/extension.test.js` (~9KB)
  even though nothing in `main`'s `require` graph ever loads it at runtime. Harmless, but worth
  a `.vscodeignore` line.
- `kb/CODE-MAP.md` was regenerated and is present; spot-checking it against the real files, its
  "Imports: (none)" lines for `src/doctor.ts`/`src/extension.ts` undercount (both really do
  import from sibling local modules) — this is the plugin's own `code-map.mjs` static-scan
  tool, marked "do not hand-edit," not something T-2's code controls; noted for completeness,
  not held against this ticket.

## Angle 7 — Ran it myself (all output reproduced, not assumed)

```
$ npm run typecheck        # tsc -p tsconfig.json --noEmit — clean, no output, exit 0
$ npm run lint             # eslint . — clean, no output, exit 0
                            #   verified this is a REAL check, not a no-op: ran `npx eslint . --format json`
                            #   → 9 files actually linted (all src/test/*.ts + *.mjs configs), 0 errors/warnings;
                            #   then appended a deliberately-broken snippet to a scratch copy of src/core.ts,
                            #   reran eslint on just that file, got 2 real @typescript-eslint/no-unused-vars
                            #   errors, then restored the file and confirmed `git diff --stat src/core.ts` is empty.
$ npm test                 # DISPLAY=:1 set → runs `npx vscode-test` directly
    build-tutorials extension
      ✔ activates and registers its two commands (111ms)
      ✔ loads the core from the sibling checkout and calls baseCss() (AC2)
      ✔ doctor reports the real sibling core: found, modules, grammars, claude (AC3)
      ✔ doctor reports a missing core honestly when repoTourPath points nowhere (AC3)
    4 passing (164ms)
$ env -u DISPLAY npm test  # DISPLAY unset → runs under `xvfb-run -a npx vscode-test`
    4 passing (87ms)       # same 4 tests, same result
$ ./build-tutorials doctor
    core found at /home/corgea/Desktop/Coding Projects/repo-tour, v0.1.0 — loaded via dist paths
      (repo-tour has no exports map yet)     [pre-T-11 state]
    modules (7 expected): all 7 ok
    grammars: ok — tree-sitter initialised; 4 grammars loaded
    claude: found at /home/corgea/.local/bin/claude
    exit 0
$ ./build-tutorials doctor /this/path/does/not/exist
    core NOT found — no repo-tour package.json found at /this/path/does/not/exist — check the
      buildTutorials.repoTourPath setting
    modules: (none attempted — core was not found)
    grammars: FAILED — core not found — cannot initialise tree-sitter
    claude: not found — core not found — cannot ask the llm module
    exit 1
$ env -i HOME=<fake> PATH=/usr/bin:/bin node scripts/doctor-cli.mjs   # forces claude to be genuinely absent
    claude: not found — not found on PATH or in known install locations (set BUILD_TUTORIALS_CLAUDE to override)
    exit 0  (core/modules/grammars all still ok — claude alone doesn't fail the doctor)
$ npm run package          # vsce package --no-dependencies
    Packaged: build-tutorials-0.0.1.vsix (11 files, 27.95 KB) — LICENSE.txt, NOTICE,
      build-tutorials, package.json, readme.md, start.sh, dist/{doctor,extension}.js,
      dist/test/extension.test.js — no node_modules, reproduces the handoff's documented fix.
```

**Mid-review environment change** (not caused by me or by this ticket): while testing, repo-tour's
T-11 merged to `main` in the sibling checkout, adding its `exports` map. I reran the full test
suite and both doctor invocations afterward — `via` correctly flipped from `dist` to
`exports`, still 7/7 modules, still 4/4 tests, with zero code changes on this branch. See
Angle 2.

Not run, per instructions (would open a GUI): `./build-tutorials dev`, `./start.sh`. Verified
by code reading and, for `start.sh`'s pause behavior specifically, by diffing against
repo-tour's real `start.sh` instead of taking the handoff's pty-harness account on faith.

## Angle 8 — Hallucination check

Every specific, checkable claim in `.autodev/handoffs/T-2.md` was checked against reality
rather than assumed:
- Module/grammar/test counts, exact test names, exact doctor wording — match my own runs
  exactly (including down to "7 exports: DEFAULT_SKIN, SKINS, ...").
- The `vsce package` error and the `--no-dependencies` fix — reproduced (Angle 7).
- repo-tour's actual licensing practice (LICENSE.txt, README section, no per-file headers, no
  NOTICE) — confirmed by reading repo-tour's own files directly, not taken on the handoff's word.
- `node_modules/repo-tour` as a symlink to the sibling checkout — confirmed (`lrwxrwxrwx ...
  repo-tour -> ../../repo-tour`).
- The machine facts (`codium` snap present, no `code`, `DISPLAY=:1`, `xvfb-run` present) — all
  match `which`/`env` output.
- The one place the handoff flags uncertainty about itself (the ledger snapshot showing
  `spec_ready: false` against its own dispatch instructions) is disclosed, not hidden, and I
  did not find anything the handoff overclaimed. No hallucinations found.

## Summary of findings

**Gating:** none.

**Advisory:**
1. `scripts/doctor-cli.mjs`'s no-argument default takes the empty-setting/node-resolution
   branch of `resolveCoreRoot`, not the `../repo-tour`-relative-path branch its own comment
   describes — provably a different code path, but not a different *outcome* under any
   `npm install`/`npm ci`-produced layout (see Angle 2 for the full trace and why it's waived
   from gating). Worth a one-line comment or logic tidy, not a rework cycle.
2. `EXPECTED_GRAMMARS` (doctor.ts) and repo-tour's `GRAMMAR_FOR` (extract.ts) keys must stay in
   sync for "a clean resolve ⇒ 4 grammars" to hold; nothing would catch a future drift, since
   `initParsers` silently skips names it doesn't recognize rather than erroring (Angle 3).
3. `build-tutorials` and `start.sh` carry no SPDX header (everything else does); moot either
   way since repo-tour itself carries none of these headers at all (Angle 4/6).
4. `.vscodeignore` doesn't exclude `dist/test/**`; the packaged `.vsix` ships the compiled test
   bundle (~9KB, never loaded at runtime) (Angle 6).
5. `kb/CODE-MAP.md`'s auto-generated import edges undercount local imports for
   `doctor.ts`/`extension.ts` — a limitation of the plugin's own `code-map.mjs`, not this
   ticket's code (Angle 6).
6. AC4 (a green CI run on `main`) is not independently verifiable pre-merge by either the
   builder or this review — correctly disclosed by the handoff, not silently skipped, and
   maximally de-risked short of an actual push (Angle 5).

**Waivers:** advisory item 1 above was seriously considered as gating ("a real bug" is
explicitly a gating category) before being traced to its root cause and downgraded — recorded
in full in Angle 2 rather than silently softened, so the reasoning is auditable.

No failing tests, no violated negative ACs (out-of-scope items — start screen, publishing,
editing repo-tour, touching the ledger — all genuinely absent from the diff), no DO-NOT-TOUCH
violations, no secrets, no security concerns.
