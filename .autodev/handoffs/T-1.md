# Handoff — T-1, Build tutorials v1 (direction)

**Written:** 2026-09-04, end of the shaping session.
**State:** direction COMPLETE (routed). The set exists (T-2 … T-10 here; T-11 … T-13 in
repo-tour) and is **waiting on Evan's approval of the SET** plus his answer to the exception
question (spec §12). No child ticket has started. `tracker.mjs ready` says T-2.
**Next actor:** whoever picks this up. Read `.autodev/specs/T-1-build-tutorials-v1.md`
first — it is the whole design; the ticket specs are its §8 row by row.

## Where things stand

- Spec: `.autodev/specs/T-1-build-tutorials-v1.md` (13 sections; §6 = the eleven locked
  decisions — do not re-ask them; §9 = the ten success criteria with their checks).
- The set, in order of play: **repo-tour T-11 ∥ T-12 ∥ T-2** → repo-tour T-13 → T-3 → T-4 →
  (T-5 ∥ T-6) → T-7 → T-8. T-9 (Mode A) and T-10 (divergence) are direction tickets parked
  at dir-goals with a running-start spec.
- Cross-shop dependency: **T-3 must not start until repo-tour T-11, T-12, T-13 are merged.**
  The tracker cannot express it with `--after`; it is written in T-3's spec.
- repo-tour T-10 (tutor) was closed as a duplicate of T-3's Ask panel; its one real gap
  (one transcript per stop) is carried by T-6 here.
- Nothing is committed in this repo beyond the initial license commit — the spec, ledger and
  this handoff are on disk only. Commit when Evan says.

## The risky part to look at first

**T-2's doctor is the whole bet.** If repo-tour's core (ESM, `import.meta.url`, tree-sitter
WASM) does not load cleanly in the extension host via dynamic `import()`, the §3 architecture
needs the server alternative instead. Prove that before T-3 spends anything. repo-tour T-11's
injectable asset roots are the enabler.

## Open with Evan

1. Approve the set (or change it — movement is free: `loopback`, `close`, `retitle`).
2. The exception question: per-ticket spec gates (seven stops) or a branch grant with only
   `accept` stopping for him. Record verbatim with `tracker.mjs grant` — never paraphrase.
