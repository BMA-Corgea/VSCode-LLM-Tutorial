# Plan — T-8 (branch `feature/T-8-package`, worktree `../VSCode-LLM-Tutorial-T-8`, cut from `main` after T-7 merges)

1. **`scripts/package.mjs`.** `stage/` (gitignored): copy `package.json`, `dist/`, `media/`, `README.md`, `LICENSE`, `NOTICE`, `.vscodeignore`; rewrite the staged `package.json`'s `repo-tour` dependency to the packed tarball path; `npm pack` the sibling repo-tour into `vendor/`; `npm install --omit=dev` in the stage; `vsce package` in the stage → `dist-vsix/`; `--verify` lists the archive (`unzip -l`) and asserts `extension/node_modules/repo-tour/dist/skins.js`, `extension/node_modules/repo-tour/schema/build-plan.schema.json`, `extension/node_modules/tree-sitter-wasms/out/tree-sitter-typescript.wasm`, and NO `extension/dist/test/`. Verify: run it; the listing assertions pass.
2. **`.vscodeignore` + `package.json`** metadata (`publisher: bma-corgea`, `repository`, `icon`, `categories: ["Education"]`, `keywords`, `license: AGPL-3.0-or-later`); `package` script → the new script; `build-tutorials package`/`publish` verbs. Verify: `vsce ls` from the stage shows the expected set.
3. **Install on both editors.** `codium --install-extension dist-vsix/*.vsix --force`; run `Build Tutorials: Doctor` from the installed copy with the `repoTourPath` setting unset → `via: exports`, core under the extension's own `node_modules`; repeat with `code` only if present (it is not on this machine — say so). Verify: the doctor output pasted into the handoff.
4. **`scripts/publish.mjs`** — dry-run prints the two commands and the `.vsix` path; real runs gated on `VSCE_PAT` / `OVSX_PAT`. CI: package + verify + dry-run publish + upload artifact. Verify: workflow green after merge (the orchestrator checks the run).
5. **README** — install (marketplace / Open VSX / `.vsix`), the first-target walkthrough on sql-gauntlet, the dial, tutor, detour, skins, AGPL + Source link. Verify: file review.
6. **Close-out.** `npm run typecheck && npm run lint && npm test`; code map regen; handoff with `Touched-files:`; `kb/CURRENT-WORK.md` line.

Risk to write-ahead: **step 1 — `vsce` refusing a `file:`/tarball dependency or `npm install` in the stage rebuilding native bits.** Neither dependency is native (WASM); if `vsce` complains about the dependency spec, use `--no-dependencies` with the stage's `node_modules` already present (vsce packages whatever is in the folder that `.vscodeignore` allows).

Negative bands: no source changes beyond packaging; no publishing without tokens; never touch the ledger.
