# Plan — T-6 (branch `feature/T-6-tutor`, worktree `../VSCode-LLM-Tutorial-T-6`, cut from `main` after T-5 merges)

Delivery target: trunk-only → local ticket branch with commits; the orchestrator merges to `main`.
**First step: reconcile `.autodev/locates/T-6.md` against the merged T-4 + T-5 code; note differences in the handoff.**

1. **`src/tutor/context.ts`** (no `vscode`). `renderOptionsText(step)` → one line per option (`[author] label — consequence`, `[alternative] …`); `tutorContext(step, plan, refRoot, target, git)` → `AskContext` with `repo` (basename of `refRoot`), `file`, `fileMeaning` (the why + its source), `stopTitle` (question), `stopText` (options text), `build: { question, options: step.decision.options.map(({label, consequence, taken}) => …), why, learnerDiff }` where `learnerDiff` is `git diff --no-index --` of the reference file vs the target file (exit code 1 is "differs", not an error), clipped to 8,000 chars; when the target file is absent, `learnerDiff = '(the learner has not created this file yet)'`. Verify: unit tests — the core's `buildContextBlock(ctx)` output contains the question, both option kinds, and the diff; absent-file text.
2. **`src/tutor/transcript.ts`** (no `vscode`). Pure functions over `Progress`; `trimMessages` from the core applied when building the prompt, never to the stored transcript. Verify: round-trip through `saveProgress`/`loadProgress`; switching steps yields the right transcript.
3. **`src/tutor/ask.ts`** (no `vscode`). `TutorDeps { core: { ask, llm }; provider; model; cwd; timeoutMs; run?: typeof runLlm }`. `checkReachable(deps)` → `providerById(provider).available()`; `askTutor(deps, ctx, messages, signal?)` → `buildAskPrompt(trimMessages(messages), ctx)` → `(deps.run ?? deps.core.llm.runLlm)(prompt, resolveChoice({provider, model}), cwd, timeoutMs)` → `{ reply: text }`, `{ unreachable: detail }`, or `{ timedOut: true }`; an `AbortSignal` cancels by rejecting the pending promise (the child is killed through `killLlmChildren` if the core exposes it — read `src/llm.ts`). Verify: unit — injected runner returns canned text; `available()` false → unreachable with the provider's detail; a runner that never resolves + a short timeout → `timedOut`.
4. **Panel + protocol + session.** `step-view.ts`: the tutor section — transcript bubbles (escaped; ``` fences → `<pre>`, nothing else), an input + *Ask* (Enter sends, Shift+Enter newline — the sql-gauntlet pattern), the reachability line, "thinking…" with *Cancel*; `step-protocol.ts`: `tutor:ask { text: string }` (non-empty, ≤ 4,000 chars), `tutor:cancel`, both through `parseInboundMessage`; `session.ts`: on `tutor:ask` → append the user turn, save, render "thinking…", `askTutor`, append the reply (or the unreachable/timeout text as an assistant turn marked `system: true`? — no: keep `AskMessage` pure; render unreachable/timeout as a `note`, not a turn), save, render. Verify: unit render tests (fence-only markup, escaping probe with `<img onerror>`), protocol probes, a session test with a fake runner.
5. **`src/extension.ts`.** Build `TutorDeps` from `resolveLlmSettings(core)` + `realGit` + `cwd = target`; `timeoutMs` 120 s (a setting `buildTutorials.tutorTimeoutSeconds`, default 120). Verify: activation test.
6. **`test/tutor.test.ts`** (vscode-test). Open a walk on a fixture plan; inject a runner through a test seam on the session (the same style as T-3's `ResumeDeps`); `tutor:ask` → the reply appears in the panel state and in `progress.json` under that step; switch step → empty transcript; switch back → restored; with `BUILD_TUTORIALS_CLAUDE=/nonexistent` and provider `claude` → the unreachable reason is shown and no "thinking…" state remains. Verify: green under `DISPLAY=:1` and `xvfb-run -a`.
7. **README + close-out.** The tutor; `npm run typecheck && npm run lint && npm test`; code map regen; handoff with `Touched-files:` and reconciliation notes; one line in `kb/CURRENT-WORK.md`.

Risk to write-ahead: **step 3 — cancelling a spawned `claude`.** Read `src/llm.ts` for `killLlmChildren` before wiring cancel; if the core cannot kill one child selectively, cancel only detaches the UI and says the model is still finishing — honest, not a hang.

Negative bands: no detour (T-7); no persona or prompt edits in repo-tour; no real model in any test; never touch the ledger.
