# Plan — T-3 (branch `feature/T-3-start-screen`, worktree `../VSCode-LLM-Tutorial-T-3`, cut from `main` AFTER repo-tour T-13 merges)

Delivery target: trunk-only → local ticket branch with commits; the orchestrator merges to `main`.

1. **Core surface.** `src/core.ts`: add `'build'` to `CORE_MODULE_NAMES`; `src/doctor.ts` expected count → 8; fix the T-2 test. Verify: `./build-tutorials doctor` shows `build: ok — … buildPlan, interpretPlan, check, stubFile …`.
2. **The skin contract, extension side.** `src/skins.ts`: `bridgeCss()` maps repo-tour's base tokens (`--bg --canvas --ink --muted --line --accent --chip --hl --hl-line --kw --str --com --num --fn --dec --field-bg --field-edge --focus --focus-ring --btn-*`) to `--vscode-editor-background`, `--vscode-sideBar-background`, `--vscode-foreground`, `--vscode-descriptionForeground`, `--vscode-panel-border`, `--vscode-textLink-foreground`, `--vscode-input-background`, `--vscode-input-border`, `--vscode-focusBorder`, `--vscode-button-*` … under `:root:not([data-theme])` so **System = follow the editor theme**; `readSkin`/`writeSkin` on `context.globalState` (key `buildTutorials.skin`, default `DEFAULT_SKIN`); `pickerHtml()` from the core's `SKINS`. Verify: unit test — the bridge sheet names every base token once; a fixture skin file dropped into `repo-tour/assets/skins/` + one `SKINS` row appears in `renderPage` output with no extension change (AC6 — do this test against a temp copy of repo-tour's assets via `configureAssets`, never by editing repo-tour).
3. **`src/webview/page.ts`.** `renderPage`: nonce CSP (`default-src 'none'; style-src 'nonce-…'; script-src 'nonce-…'`), `<html data-theme="…">` stamped by the host unless `system`, inline `<style>` = base + alternates + bridge, the page body, one inline `<script nonce>` with `acquireVsCodeApi()`, `post(type, payload)`, `restore()`/`save()` over `getState/setState`, and the skin picker wiring (posts `skin:set`; the host re-renders). Verify: unit test extracts every `<script>` and runs `new Function(code)` — parse only (repo-tour's lesson); CSP header present.
4. **`src/start/request.ts`.** `BuildRequest`, `Dial = 'manual' | 'scaffolded' | 'automated'`, `validateRequest` (pure): `recreate && !repo` → `repo: "required when recreating"`; `!recreate && idea && !repo` → `idea: "Idea-first builds arrive with T-9 — tick 'recreate' to build from a repo"`; `!recreate && !idea` → `idea: "say what you want to build, or tick recreate"`; `!target` → `target: "pick an empty or new folder"`; repo must be an absolute path that exists or a `https://github.com/<o>/<r>` URL. Verify: unit tests, one per rule.
5. **`src/start/panel.ts`.** `StartPanel.show(context)` (singleton `WebviewPanel`, `ViewColumn.Active`, `enableScripts`, `retainContextWhenHidden: false`; on `onDidChangeViewState` visible → re-render from host state). The form per spec §5.2 (idea textarea, recreate checkbox, repo input + *Browse…* → `pick:repo` (`showOpenDialog` folders), target input + *Browse…* → `pick:target`, the dial as three radio buttons, *Build the plan* button, the skin picker in the corner). `form:changed` → host validates → posts `form:problems` (the message is shown under the field, where the eye is — never a silent disable); `form:submit` → validate → `buildFromRequest`. Verify: vscode-test opens the panel, posts a `form:submit` with recreate + no repo, asserts the `form:problems` reply names `repo`.
6. **`src/start/build.ts`.**
   a. `resolveRepo(req, ctx)`: path → as is; URL → `git clone <url> <globalStorageUri>/refs/<owner>-<name>` (full clone — the witness needs history; reuse if present, `git fetch` otherwise). Progress reported.
   b. language check: run `core.digest.digest(repoRoot, { write: true })`; if no `source` file's language is in `{python, javascript, typescript, tsx}` → refuse with `"<repo> is <langs>; the grammars repo-tour ships cover TS, JS, TSX and Python"` shown in the form.
   c. write-ahead marker `<target>/.repo-tour/build/building.json` `{ startedAt, phase, request }` updated at each phase (`digest` → `plan` → `interpret` → `write`); `withProgress` notification (`ProgressLocation.Notification`, cancellable: cancel removes the marker and says so).
   d. `buildPlan(digest, { root: repoRoot, witness: true })` → `interpretPlan(plan, digest, { root, provider, model, cacheDir: core.interpret.defaultCacheDir(), onProgress })` → write `<target>/.repo-tour/build/plan.json` and `request.json`; delete the marker; `showInformationMessage` with `N chapters · M steps` and the cost (`metered ? "$x.xx, y tokens" : "this provider does not report usage"`); `workspaceState.update('buildTutorials.currentBuild', target)`.
   e. `resumeIfMarked(ctx)`: on activation, if the remembered target has a marker younger than 6 h, offer *Resume* / *Discard* — resume re-runs from the marker's request (every finished interpretation is cached, so it is cheap).
   Verify: vscode-test end-to-end on a synthetic TS fixture repo with `cachedOnly: true` (no model) → `plan.json` exists, validates against `repo-tour/schema/build-plan.schema.json` (hand-rolled checker, same as T-12's test), `request.json` matches; a Go-only fixture is refused with the reason; a stale marker test: write a marker, activate, assert the prompt appears (`showInformationMessage` stubbed via a test hook) and resume writes the plan.
7. **`src/extension.ts` + `package.json`.** `start` → `StartPanel.show`; activation → `resumeIfMarked`; settings `buildTutorials.llmProvider` (`claude`), `buildTutorials.llmModel` (default from the core's `DEFAULT_MODEL`). Verify: activation test still green; settings appear in the Settings UI (`contributes.configuration`).
8. **Tests wiring.** `esbuild.mjs`: add a `test/unit/*.test.ts` bundle → `dist/test/unit/`; `package.json` `test:unit` (plain mocha, no VS Code) and `test` runs both. Verify: `npm test` green under `DISPLAY=:1` and `xvfb-run -a`.
9. **README.** The start screen, the dial, where the plan lands (`<target>/.repo-tour/build/`), the skin contract ("one CSS file in repo-tour, both front ends"). Verify: file review.
10. **Close-out.** `npm run typecheck && npm run lint && npm test`; code map regen; handoff `.autodev/handoffs/T-3.md` with `Touched-files:`; one line in `kb/CURRENT-WORK.md`.

Risk to write-ahead: **step 6d — a full `digest` + `interpretPlan` on a real repo takes minutes and spends tokens.** Tests use `cachedOnly`; the one live run is the acceptance target (sql-gauntlet), done by the orchestrator, not the suite.

Negative bands: no walk, no tree, no step panel (T-4); no file writing into the target beyond `.repo-tour/build/` (T-5 writes code); never edit repo-tour; never touch the ledger.
