# Plan — T-5 (branch `feature/T-5-dial`, worktree `../VSCode-LLM-Tutorial-T-5`, cut from `main` after T-4 merges)

Delivery target: trunk-only → local ticket branch with commits; the orchestrator merges to `main`.
**First step of the build: reconcile every name in `.autodev/locates/T-5.md` against T-4's merged code and note the differences in the handoff.**

1. **`src/dial/writer.ts`** (no `vscode`). `planWrite(step, plan, dial, refRoot, target, core)`: `file` steps only — manual → `null`; scaffolded → read `<refRoot>/<file>`, `core.build.stubFile(source, step.scaffold.loadBearing, language, questions)` where `questions` map each range to its symbol step's `decision.question` and ordinal (read `stubFile`'s real signature/`StubQuestion`); automated → the source verbatim. `fileShape(refRoot, step)` → `{ signatures: string[] (first line of each load-bearing range), imports: string[] (lines matching import/require/from … import) }`. `reproduceFiles(plan, refRoot, target)` copies each `plan.reproduce` path byte-for-byte (mkdir -p), returns the list. Verify: unit tests on a fixture reference with a TS, a JS and a Python file — the scaffolded output parses with zero ERROR nodes through the core's `extract`; automated output equals the source; manual returns null.
2. **`src/dial/check.ts`** (no `vscode`). `referenceExtract(core, refRoot, relPath)` (inventory-shaped `FileRecord` + `core.extract.extract`, exactly as repo-tour's `test/build.test.ts` does for its `check` tests); `runCheck(core, refRoot, target, step)` → `{ report: CheckReport; summary: string }` ("3 of 3 exports present, 2 imports, parses clean" / "missing export: rank"). Verify: unit — a learner file with a renamed local passes; a missing export is named; a parse failure carries the core's `reason`.
3. **`src/dial/commit.ts`** (no `vscode`). `ensureRepo(target, git)` (`git init -q` when no `.git`); `identityConfigured(target, git)` (`git config user.name` + `user.email` non-empty); `commitStep(target, step, files, git)` → adds the files, commits `"<ordinal>: <question> — <label of the option with taken:true>"`; returns `{ committed: false, reason: 'no git identity configured in this folder' }` rather than inventing one. Verify: unit — a temp target with identity gets the commit (assert `git log -1 --format=%s`); without identity, no commit and the reason.
4. **Session hooks (`src/walk/session.ts`).** On `goTo(step)`: resolve the step's dial (per-step override, else the request's global), for a `file` step call `planWrite`; if the target file exists and differs → `confirm('Overwrite / Keep mine')` (injected, defaults to `showWarningMessage`); write via `writeJsonAtomic`-style temp+rename for text; reveal. In automated mode at the first `file` step run `reproduceFiles` first (once; `progress.reproduced`). `check:run` → `runCheck` → store `progress.steps[id].check`, refresh tree, render the report; on `ok` → `markDone` + `commitStep` when `buildTutorials.commitPerStep`. Automated `symbol` step: the panel narrates (why/summary) with the range decorated; *Next* → `markDone` + commit of that file if not yet committed. Verify: unit test of the session state machine with fake adapters (fake git, fake confirm) covering all three dials.
5. **Panel + tree.** `step-view.ts`: the check report block; the overwrite/keep note; the reproduce offer at the first shape step ("N files aren't taught — copy them now?" + button); the manual-mode shape block (signatures + imports). `step-protocol.ts`: `check:run`, `reproduce:run` validated by `parseInboundMessage`. `tree.ts`: `$(pass)` / `$(error)` from `check`. Verify: unit render tests per dial; script parse test; protocol probes (null payload etc.).
6. **`src/extension.ts` + `package.json`.** Command `buildTutorials.reproduceFiles`; setting `buildTutorials.commitPerStep` (boolean, default true). Verify: activation test.
7. **`test/dial.test.ts`** (vscode-test). A synthetic reference repo (3 files: TS + JS + a generated `dist/out.js` + a `data.csv`) → plan via the core with `cachedOnly` → target folder → walk every step in **automated** mode → `diff -r target reference` (ignoring `.repo-tour/`, `.git/`) is empty (AC6, T-1 §9 #3); the target has one commit per completed file step with the expected subjects; a **scaffolded** run writes stubs that parse; a **manual** run writes nothing and the panel shows the shape; a check on an intentionally broken file marks ✗ in the tree. Verify: green under `DISPLAY=:1` and `xvfb-run -a`.
8. **README + close-out.** The dial in practice; `npm run typecheck && npm run lint && npm test`; code map regen; handoff `.autodev/handoffs/T-5.md` with `Touched-files:` and the reconciliation notes; one line in `kb/CURRENT-WORK.md`.

Risk to write-ahead: **step 4 — the overwrite prompt inside a `goTo` that the tree can trigger repeatedly.** Debounce: never prompt twice for the same (step, file) within a session; write the handoff before wiring it.

Negative bands: no tutor (T-6); no detour (T-7); no edits under `src/start/`; never edit repo-tour; never touch the ledger; never write outside `<target>`.
