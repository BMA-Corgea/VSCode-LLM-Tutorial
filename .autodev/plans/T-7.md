# Plan — T-7 (branch `feature/T-7-detour`, worktree `../VSCode-LLM-Tutorial-T-7`, cut from `main` after T-6 merges)

Delivery target: trunk-only → local ticket branch; the orchestrator merges. **First step: reconcile `.autodev/locates/T-7.md` against merged code.**

1. **`src/detour/prompt.ts`.** `DETOUR_PROMPT_VERSION = 1`; `detourPrompt(step, plan, refName)` — states the concept (question + why + the author's option), the learner's domain (repo name, file role) to AVOID, and the exact JSON shape; `parseDetourReply(raw)` tolerant of fences, strict on shape and limits, rejects unsafe paths. Verify: unit — a good reply parses; > 5 files / > 60 lines / 2 stops / `../x` / absolute path each rejected with a reason.
2. **`src/detour/store.ts`.** Directory + cache helpers; atomic writes; `removeDetour` is `rm -rf` of exactly `detourDir(...)` and refuses any path not under `globalStorageRoot/detours/`. Verify: unit — round-trip; the refusal.
3. **`src/detour/narrator.ts`.** A webview tab via `renderPage` (skin contract, nonce CSP, parse-tested script) listing the stops, the current one highlighted, *Previous / Next / Back to my build*; messages validated. Verify: unit render + script parse; protocol probes.
4. **`src/detour/session.ts`.** `start`: reachability (T-6's) → cache or ask via the injected runner → parse → write → open tabs (`preview: false`, `preserveFocus`) → narrator → `goto(0)` (reveal + decorate that stop's range in its tab). `return`: close only detour tabs (`window.tabGroups.all` → tabs whose `input` URI starts with the detour dir), dispose narrator, `removeDetour`, re-reveal the walk's step, append `{ kind: 'note', content: 'detour taken: <title>' }` to the step's transcript, save. Verify: unit state machine with fake adapters (counts tabs closed, dir removed).
5. **Panel wiring.** The button beside *Ask*; `detour:start` → `DetourSession.start`; the note renders in the transcript. Verify: render test; protocol probe.
6. **`test/detour.test.ts`** (vscode-test). Open a walk on a fixture plan; hash the workspace tree; inject a runner with a canned 3-file/3-stop detour; `detour:start` → files exist under `globalStorageUri/detours/<stepId>/`, ≥ 3 detour tabs open, narrator open, workspace tree hash unchanged; `return` → those tabs closed, directory absent, current step unchanged, transcript note present, workspace hash still unchanged; second `start` on the same step → zero runner calls (cache). Verify: green under `DISPLAY=:1` and `xvfb-run -a`.
7. **README + close-out.** `npm run typecheck && npm run lint && npm test`; code map regen; handoff with `Touched-files:`; one line in `kb/CURRENT-WORK.md`.

Risk to write-ahead: **step 4 — closing tabs programmatically** (`window.tabGroups.close(tabs)`) and whether an untitled-vs-file URI comparison holds on this platform; write the handoff before, and if a tab cannot be closed the return still deletes the directory and tells the learner which tab to close.

Negative bands: never write into the workspace; no model in tests; no persona/prompt edits in repo-tour; never touch the ledger.
