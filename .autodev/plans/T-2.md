# Plan — T-2 (branch `feature/T-2-skeleton`, worktree `../VSCode-LLM-Tutorial-T-2`)

Delivery target: trunk-only (verification-model default) → a local ticket branch with commits; the orchestrator merges to `main`.

1. **Manifest + toolchain.** `package.json`: name `build-tutorials`, publisher `bma-corgea`, `engines.vscode ^1.95`, `main: ./dist/extension.js`, `activationEvents` on the two commands, `contributes.commands` (`buildTutorials.start`, `buildTutorials.doctor`), `contributes.configuration.buildTutorials.repoTourPath` (string, default `../repo-tour`, described as "path to the repo-tour checkout, relative to the extension or absolute"). devDeps: typescript, esbuild, @types/vscode, @types/node, @types/mocha, mocha, @vscode/test-cli, @vscode/test-electron, @vscode/vsce, eslint + typescript-eslint. Runtime dep: `"repo-tour": "file:../repo-tour"`. `tsconfig.json` mirrors repo-tour's strictness (`strict`, `noUncheckedIndexedAccess`). Verify: `npm install` links `node_modules/repo-tour` → the sibling checkout.
2. **Bundle.** `esbuild.mjs` bundles `src/extension.ts` → `dist/extension.js` (cjs, platform node, `external: ['vscode', 'repo-tour']`), sourcemaps, `--watch` flag for dev. Verify: `npm run build` emits the file.
3. **`src/core.ts`.** `resolveCoreRoot(setting, extensionRoot)` → absolute dir (setting absolute as-is; relative to the extension root; else `require.resolve('repo-tour/package.json')`). `loadCore(root)` dynamic-`import()`s `skins`, `extract`, `llm`, `digest`, `interpret`, `ask`, `types` by trying `repo-tour/<mod>` then `pathToFileURL(<root>/dist/<mod>.js)`; returns `{ root, version, modules: {…}, via: 'exports'|'dist' }`. Never bundles the core (esbuild `external`). Verify: a unit test loads it from the sibling path.
4. **`src/doctor.ts`.** `runDoctor(ctx)` → `DoctorReport { core: {found, root, version, via}, modules: {name, ok, detail}[], grammars: {ok, loaded: string[], detail}, claude: {ok, path|reason}, summary }`, written to an output channel "Build Tutorials" in plain sentences ("core found at … v0.1.0, loaded via dist paths (repo-tour has no exports map yet)"). Grammars: `await extract.initParsers()` then confirm the four. Claude: `llm.resolveBin('claude', 'BUILD_TUTORIALS_CLAUDE')`. A missing core is a report, never a throw. Verify: tests in step 6.
5. **`src/extension.ts`.** `activate` registers both commands; `start` shows `window.showInformationMessage('The start screen arrives with T-3 — run "Build Tutorials: Doctor" to check this machine.')` — honest, not silent. Verify: activation test.
6. **Tests.** `.vscode-test.mjs` (test-cli) → `test/extension.test.ts`: (a) activates; (b) doctor with the real sibling core → core.found, ≥ 6 modules ok, 4 grammars, claude found-or-reason; (c) with `repoTourPath` = `/nonexistent` → core.found false, reason names the path, no throw. `npm test` runs `xvfb-run -a vscode-test` when `DISPLAY` is unset. Verify: green locally.
7. **CI.** `.github/workflows/ci.yml`: two checkouts side by side (`path: VSCode-LLM-Tutorial` and `repository: BMA-Corgea/repo-tour`, `path: repo-tour`), node 22, build repo-tour (`npm ci && npm run build`), then in the extension `npm ci`, typecheck, lint, `npm test` (xvfb), `vsce package --no-dependencies`? — NO: dependencies must ship (the core is a runtime dep), so plain `vsce package` and upload the `.vsix` artifact. Verify: workflow file lints; a green run after merge.
8. **Scripts.** `build-tutorials` (bash, executable): `dev [folder]` (build, pick `codium`→`code`, `--extensionDevelopmentPath="$ROOT"` on the folder, default `$ROOT/.scratch/<date>` created empty), `build`, `test`, `doctor` (a node script `scripts/doctor-cli.mjs` that loads `dist/` doctor logic without VS Code — factor `runDoctor`'s core so it needs no `vscode` import), `package`. `start.sh` wraps `dev` with the pause. Verify: `./build-tutorials doctor` prints the report on this machine; `./start.sh` opens codium.
9. **Docs + license.** `README.md` (what it is, the doctor, `./start.sh`, Source link, AGPL notice), `NOTICE`, SPDX headers on `src/*.ts`. `.gitignore` adds `.scratch/`, `.vscode-test/`.
10. **Close-out.** `node <plugin>/scripts/code-map.mjs --root .` regenerates `kb/CODE-MAP.md`; handoff `.autodev/handoffs/T-2.md` with a `Touched-files:` line; one line in `kb/CURRENT-WORK.md`.

Risk to write-ahead before attempting: **step 3/4 — does web-tree-sitter initialise inside the extension host?** If `Parser.init()` fails there, record the exact error in the handoff and stop with STALLED; that is the §3 architecture question, not something to work around.

Negative bands: no user-facing screen; no publish; never edit repo-tour; never touch the ledger.
