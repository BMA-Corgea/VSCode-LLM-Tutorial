# Locate — T-6 (drafted against T-4/T-5 plans; RECONCILE against merged code before build)

## The ground
- T-4: `src/walk/session.ts` (`WalkSession`, current step, `progress`), `src/walk/step-view.ts` (`renderStepHtml`), `src/walk/step-protocol.ts` (`parseInboundMessage`, `handleStepMessage`), `src/walk/step-panel.ts`, `src/walk/store.ts` (`saveProgress` atomic).
- T-5: `src/dial/**` (the git runner use; the reference root from `request.json`).
- T-3: `src/extension.ts` `resolveLlmSettings(core)` → `{ provider, model }`; `src/webview/html.ts` `escapeHtml`.
- The core: `loaded['ask']` → `buildAskPrompt`, `buildContextBlock`, `trimMessages`, `ASK_PERSONA`; `loaded['llm']` → `resolveChoice`, `runLlm`, `providerById`, `resolveBin`, `surveyProviders`. Read repo-tour `src/ask.ts` (the `build` block T-13 added) and `src/llm.ts` (`runLlm(prompt, choice, cwd, timeoutMs)`, `Provider.available()`).

## Touched set
- `src/tutor/context.ts` (new, no vscode) — `tutorContext(step, plan, refRoot, target, git)` → `AskContext` incl. `build.learnerDiff` (clipped); `renderOptionsText(step)`.
- `src/tutor/transcript.ts` (new, no vscode) — `getTranscript(progress, stepId)`, `appendTurn(progress, stepId, msg)` (returns a new progress; the session saves it).
- `src/tutor/ask.ts` (new, no vscode) — `askTutor(deps, ctx, messages)`: availability check → prompt via the core → `deps.run` (defaults to `runLlm`) with a timeout → `{ reply } | { unreachable: reason } | { timedOut }`.
- `src/walk/step-view.ts` (edit) — the tutor section (transcript bubbles with fence-only markup, input, *Ask*, the reachability line, "thinking…" + cancel); `src/walk/step-protocol.ts` (edit) — `tutor:ask { text }`, `tutor:cancel`, validated; `src/walk/session.ts` (edit) — wires the ask, persists transcripts, re-renders; `src/walk/model.ts` (edit) — `transcript?: AskMessage[]` on the step's progress entry.
- `src/extension.ts` (edit) — passes `{ provider, model }` + the git runner into the session's tutor deps.
- Tests: `test/unit/{tutor-context,tutor-transcript,tutor-ask,tutor-view}.test.ts`; `test/tutor.test.ts` (vscode-test: an injected runner's canned reply lands in the panel state and in `progress.json`; `BUILD_TUTORIALS_CLAUDE=/nonexistent` → the unreachable reason appears with no pending state).

## DO-NOT-TOUCH
`src/start/**` · `src/dial/**` beyond reading `request.json`'s reference root · repo-tour's tree (the persona, the prompt) · `.autodev/**` ledger · the detour (T-7).

## Conflicts
Runs after T-5 (shared step panel/protocol/session). T-7 follows this ticket.
