# Locate — T-7 (drafted; RECONCILE against merged T-4/T-5/T-6 code before build)

## The ground
- T-4: `src/walk/editor.ts` (`revealStep`-style reveal + decoration helpers — generalise to `revealRange(uri, range)` if T-4 did not), `src/walk/session.ts` (current step, dial, `goTo`), `src/walk/step-view.ts` + `step-protocol.ts` (the panel where the button lives), `src/webview/page.ts` (`renderPage`).
- T-6: `src/tutor/ask.ts` (`TutorDeps`, the injected runner, reachability), `src/tutor/transcript.ts`.
- T-3: `src/start/build.ts` (`writeJsonAtomic`); `context.globalStorageUri`.
- The core: `loaded['llm']` (`runLlm`, `resolveChoice`), `loaded['ask']` is NOT used here (a different prompt — the detour prompt is this extension's own, kept in `src/detour/prompt.ts`).

## Touched set
- `src/detour/prompt.ts` (new, no vscode) — `detourPrompt(step, plan, refName)`; `parseDetourReply(raw)` → `Detour | { error }` (shape check: ≤ 5 files, ≤ 60 lines each, 3–6 stops, paths relative and safe — no `..`, no absolute).
- `src/detour/store.ts` (new, no vscode) — `detourDir(globalStorageRoot, stepId)`, `writeDetour(dir, detour)` (atomic per file), `removeDetour(dir)`, the cache (`cacheKey(stepId, PROMPT_VERSION)`, `readCached`, `writeCached`).
- `src/detour/narrator.ts` (new) — a webview editor tab: title, the domain, the stop list, *Previous / Next / Back to my build*; posts `detour:goto`, `detour:return`, validated.
- `src/detour/session.ts` (new) — `DetourSession.start(context, step, deps)`: ask (or cache) → write files → open each as a tab (`workspace.openTextDocument` + `showTextDocument`, `preview: false`) → narrator → walk stops with reveal + decoration; `return()` closes only the detour's tabs (match `tab.input.uri` under the detour dir via `window.tabGroups`), disposes, deletes the dir, re-reveals the walk's current step, appends the transcript note.
- `src/walk/step-view.ts` + `step-protocol.ts` + `session.ts` (edit) — the *Show me an unrelated example* button → `detour:start`; the "detour taken" note.
- `src/extension.ts` (edit) — wires `DetourSession` deps; command `buildTutorials.returnFromDetour` (also on the narrator).
- Tests: `test/unit/{detour-prompt,detour-store}.test.ts` (parse/validate replies incl. a path-traversal attempt; cache round-trip); `test/detour.test.ts` (vscode-test: canned reply → files under global storage, tabs open, the workspace tree hash unchanged; return → tabs closed, dir gone, current step unchanged, transcript note present).

## DO-NOT-TOUCH
`src/start/**` · `src/dial/**` · repo-tour's tree · `.autodev/**` ledger · the workspace folder (a detour never writes there — the test asserts it).

## Conflicts
Sequenced after T-6 (shares the step panel and the tutor deps). T-8 follows.
