# Locate — T-4

## The ground (T-3, verified 2026-09-05)
- `src/webview/page.ts` `renderPage({ title, coreCss, bridgeCss, pageCss?, skin, body, script })` — prepends the bootstrap (`post`, `restore`, `save`); `src/webview/html.ts` `escapeHtml`, `escapeAttr`.
- `src/skins.ts` `readSkin(store, fallback)`, `writeSkin(store, name)`, `bridgeCss()`, `pickerHtml(skins, current)`, `BRIDGE_TOKEN_MAP`.
- `src/start/panel.ts` — the adapter pattern (pure protocol + view, host state, `onDidChangeViewState` re-render, `reportProblem`, test seams `dispatchForTest`/`getStateForTest`); `src/start/protocol.ts` (`handleStartMessage(state, msg, effects)`); `src/start/view.ts` (`renderStartHtml(state, skins)`, `SkinsModule`).
- `src/start/build.ts` — `buildDir(target)`, `markerPath`, the atomic write pattern, `BuildOutcome { ok, plan, cost, reason }`, `plan.json` + `request.json` written to `buildDir(target)`.
- `src/extension.ts` — `runStartCommand` → `StartPanel.show`; `runBuild` → `withProgress` → `reportOutcome`; `resumeIfMarked` on activation; settings `repoTourPath`, `llmProvider`, `llmModel`; `activationEvents` includes `onStartupFinished`.
- `src/core.ts` `CORE_MODULE_NAMES` includes `build`; `loadCore(root).loaded['build']` → `buildPlan`, `interpretPlan`, `check`, `stubFile` (T-5 uses `check`/`stubFile`).
- `esbuild.mjs` bundles `src/extension.ts`, `src/doctor.ts`, each `test/*.test.ts`, and `test/unit/**` (unit entry points glob); `package.json` `test:unit` (mocha tdd) + vscode-test.
- The plan's shape: repo-tour `src/build/types.ts` — `BuildPlan { chapters[{key,title,subtitle,tierPath}], steps[{ id, ordinal, chapter, kind, decision{question, options[{id,label,consequence,taken}], authorChoice, chosen, why, whySource}, target{file,startLine?,endLine?}, scaffold{loadBearing[],boilerplate[]}, dependsOn[], witness{sha,date,subject} }], reproduce[], cost }`.

## Touched set
- `src/walk/model.ts` (new, no vscode) — `Progress` type + `emptyProgress(plan)`, `treeModel(plan, progress)` (chapters → step nodes with status), `currentStep`, `nextStepId`, `prevStepId`, `markDone`.
- `src/walk/store.ts` (new, no vscode) — `loadPlan(target)`, `loadProgress(target)`, `saveProgress(target, p)` (atomic), `hasPlan(target)`.
- `src/walk/step-view.ts` (new, no vscode) — `renderStepHtml(state, skins)`: question, options (author marked; alternatives with consequence, "coming with T-10"), why + source label, witness or "no history to show", dial radios, buttons, skin picker; its client script.
- `src/walk/step-protocol.ts` (new, no vscode) — `handleStepMessage(state, msg, effects)`; messages `step:next`, `step:prev`, `step:open`, `step:check`, `dial:set`, `skin:set`.
- `src/walk/tree.ts` (new) — `DecisionsTreeProvider implements vscode.TreeDataProvider`, built from `treeModel`.
- `src/walk/step-panel.ts` (new) — `StepViewProvider implements vscode.WebviewViewProvider` for `buildTutorials.step`.
- `src/walk/editor.ts` (new) — `revealStep(target, step)` + the decoration type; `clearDecorations()`.
- `src/walk/session.ts` (new) — `WalkSession`: the host's single truth (plan, progress, current); wires tree, panel, editor, status bar; `open(target)`, `goTo`, `next`, `prev`, `setDial`; persists on change; `dispose`.
- `src/extension.ts` (edit) — register the container's views + commands (`buildTutorials.openWalk`, `nextStep`, `prevStep`, `goToStep`); detect a plan in the workspace on activation; after a successful build open the walk or offer *Open the folder*.
- `package.json` (edit) — `viewsContainers.activitybar` + `views` + commands + `menus.view/title` (next/prev); a `media/icon.svg` for the container.
- `esbuild.mjs` (edit) — a `test/walk.test.ts` bundle. Tests: `test/unit/{walk-model,walk-store,step-view,step-protocol}.test.ts`, `test/walk.test.ts` (vscode-test).
- `README.md` (edit) — the walk.

## DO-NOT-TOUCH
`src/start/**` beyond the post-build hand-off in `extension.ts` · `src/doctor.ts` · `scripts/*` · `.github/workflows/ci.yml` · repo-tour's tree · `.autodev/**` ledger. No file writing into the target (T-5); no tutor (T-6).

## Conflicts
None in this shop (T-5 and T-6 both wait on this). Cross-shop: consumes repo-tour `main` as merged (no new core work needed).
