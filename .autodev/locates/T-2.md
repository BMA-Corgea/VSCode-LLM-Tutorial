# Locate — T-2

Greenfield: the repo holds `LICENSE`, `.gitignore`, `.autodev/`, `kb/`. Nothing to find; everything is new.

## Touched set (all new)
`package.json` · `tsconfig.json` · `esbuild.mjs` · `eslint.config.mjs` · `.vscodeignore` · `.vscode/launch.json`
`src/extension.ts` (activate: two commands) · `src/core.ts` (load repo-tour's ESM core) · `src/doctor.ts`
`test/` (`@vscode/test-cli` config + `extension.test.ts`) · `.github/workflows/ci.yml`
`build-tutorials` (control script) · `start.sh` · `README.md` · `NOTICE`

## Entry points
- `src/extension.ts#activate` → registers `buildTutorials.doctor`, `buildTutorials.start`.
- `src/core.ts#loadCore(repoTourPath)` → the one place the core is imported. Try the subpath exports
  (`repo-tour/skins`, arriving with repo-tour T-11) and fall back to `repo-tour/dist/<mod>.js` today.

## The ground it stands on (read, verified 2026-09-04)
- repo-tour: `package.json` (ESM, `type: module`, no `exports` map yet, `dist/` built and present,
  `private: true`), `src/skins.ts` (`baseCss/alternateCss/SKINS`), `src/extract.ts#initParsers`
  (web-tree-sitter + tree-sitter-wasms grammars: python, javascript, typescript, tsx),
  `src/llm.ts#resolveBin(name, envVar)`. Path on this machine: `/home/corgea/Desktop/Coding Projects/repo-tour`.
- Toolchain: node v22.22.2, npm 10.9.7, `codium` 1.105 (snap) — **no `code` binary**, `DISPLAY=:1`, `xvfb-run` present.

## DO-NOT-TOUCH
- `.autodev/**` ledger (`events.jsonl`, `tickets/`, `releases/`) — tracker is the single writer. Handoff + specs are fine.
- `LICENSE` — AGPL-3.0 as committed.
- repo-tour's tree — this ticket consumes it, never edits it (T-11/T-12 own that, in repo-tour).

## Conflicts
None in this shop. Cross-shop: repo-tour T-11 changes its `package.json` exports — `src/core.ts` must work before AND after.
