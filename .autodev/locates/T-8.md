# Locate — T-8

## The ground
- `package.json` (`package: vsce package --no-dependencies`, `vscode:prepublish`, `files`?), `.vscodeignore` (ships the test bundle today — T-2 review advisory), `esbuild.mjs`, `.github/workflows/ci.yml` (two side-by-side checkouts; `vsce package --no-dependencies` artifact), `build-tutorials` (the `package` verb), `README.md`, `NOTICE`, `src/core.ts` (`resolveCoreRoot`: setting → relative-to-extension → `require.resolve('repo-tour/package.json')`).
- repo-tour: `package.json` (`files`, `prepare`, exports map), `T-11`'s consumer test (the `npm pack` → install pattern is already proven there).

## Touched set
- `scripts/package.mjs` (new) — stage → `npm pack` repo-tour → install into the stage → `vsce package` from the stage → `dist-vsix/build-tutorials-<v>.vsix`; verifies the archive lists `extension/node_modules/repo-tour/dist/skins.js` and a `.wasm`.
- `scripts/publish.mjs` (new) — dry-run by default; `vsce publish` / `ovsx publish` when tokens exist.
- `.vscodeignore` (edit), `package.json` (edit: `package` → `node scripts/package.mjs`, `publish:dry`, `publisher`, `repository`, `icon`, `categories`, `keywords`), `build-tutorials` (edit: `package`/`publish` verbs), `.github/workflows/ci.yml` (edit: use the new script; upload the `.vsix`; dry-run publish), `README.md` (edit), `media/icon.png` (new — drawn, no third-party asset).
- Tests: `test/unit/package-script.test.ts` (the archive-listing check on a produced `.vsix` — run in CI after packaging) or a shell check inside `scripts/package.mjs --verify`.

## DO-NOT-TOUCH
`src/**` beyond `core.ts` comments · repo-tour's tree · `.autodev/**` ledger.

## Conflicts
Last in the set; nothing else in flight.
