# Locate — T-5 (drafted against T-4's plan; RECONCILE names against T-4's merged code before build)

## The ground (T-4 as planned)
- `src/walk/session.ts` `WalkSession` — `open`, `goTo(id)`, `next`, `prev`, `setDial(stepId, dial)`, `markDone(id)`, `dispose`; the host's single truth (plan, progress, current step).
- `src/walk/model.ts` `Progress { current, steps: { [id]: { status, dial? } } }`, `treeModel`, `nextStepId`; `src/walk/store.ts` `loadPlan`, `loadProgress`, `saveProgress` (atomic), `hasPlan`.
- `src/walk/step-view.ts` `renderStepHtml(state, skins)` — the dial radios, *Open the file*, *Check my work* (replies "arrives with T-5"), *Next*/*Previous*, the `note` line; `src/walk/step-protocol.ts` `handleStepMessage` + `parseInboundMessage` (`step:check`, `dial:set` already defined); `src/walk/step-panel.ts`; `src/walk/editor.ts` `revealStep`, decorations; `src/walk/tree.ts` (statuses/icons).
- `src/start/build.ts` `writeJsonAtomic`, `buildDir`, `realGit`/`GitRunner` (reuse for commits); `request.json` (`repo` = the reference root).
- The core: `loaded['build']` → `check`, `stubFile` (read their signatures in repo-tour `src/build/{check,stub}.ts`), `loaded['extract']` → `extract`, `initParsers`.

## Touched set
- `src/dial/writer.ts` (new, no vscode) — `planWrite(step, plan, dial, refRoot, target, core)` → `{ path, content } | null` per the notes; `fileShape(refRoot, step)` for manual mode; `reproduceFiles(plan, refRoot, target)`.
- `src/dial/check.ts` (new, no vscode) — `referenceExtract(core, refRoot, relPath)`, `runCheck(core, refRoot, target, step)` → the core's `CheckReport` (+ a one-line summary).
- `src/dial/commit.ts` (new, no vscode) — `ensureRepo(target, git)`, `commitStep(target, step, files, git)` → `{ committed: boolean; reason?: string }`.
- `src/walk/session.ts` (edit) — hooks: on `goTo` apply the dial's write (with the overwrite prompt through an injected `confirm`), narrate in automated mode, `runCheck` on `step:check`, `markDone` + commit on a passing check or a narrated *Next*; `reproduceFiles` command wiring.
- `src/walk/step-view.ts` + `step-protocol.ts` (edit) — the check report block (present / missing / extra per symbol, imports, parse errors + reason), the overwrite/keep note, the reproduce offer; messages `reproduce:run`, `check:run` (rename of `step:check` if T-4 named it so — reconcile).
- `src/walk/tree.ts` (edit) — ✓ / ✗ icons from `progress.steps[id].check`.
- `src/extension.ts` + `package.json` (edit) — command `buildTutorials.reproduceFiles`, setting `buildTutorials.commitPerStep`.
- Tests: `test/unit/{dial-writer,dial-check,dial-commit}.test.ts`; `test/dial.test.ts` (vscode-test: the automated byte-for-byte walk; scaffolded stub parses; a manual step writes nothing; check ✓/✗ on the tree).

## DO-NOT-TOUCH
`src/start/**` · `src/walk/tree.ts` beyond icons · repo-tour's tree · `.autodev/**` ledger · the tutor (T-6) and detour (T-7) — T-6 runs AFTER this ticket to avoid a three-way edit of the step panel.

## Conflicts
T-6 is sequenced after T-5 (both edit `step-view.ts` / `step-protocol.ts` / `session.ts`). Nothing else in flight.
