# Locate — T-3

## The ground (T-2, merged `fd97c8e`; verified 2026-09-04)
- `src/core.ts` — `CORE_MODULE_NAMES` (`skins, extract, llm, digest, interpret, ask, types`), `loadCore(root): CoreLoadResult { found, root, version, via, modules, reason, loaded }`, `resolveCoreRoot(setting, extensionRoot)`.
- `src/doctor.ts` — `runDoctor(root)`, `formatReport`; no `vscode` import (headless-safe).
- `src/extension.ts` — `activate` registers `buildTutorials.doctor` and `buildTutorials.start` (the latter is the honest placeholder this ticket replaces).
- `esbuild.mjs` — three CJS bundles (`extension`, `doctor`, `test`), `external: ['vscode', 'repo-tour']`. `.vscode-test.mjs` + `scripts/run-tests.mjs` (xvfb when no `DISPLAY`). `test/extension.test.ts` (mocha `suite/test`, 60 s timeout).
- `package.json` — `contributes.commands`, `contributes.configuration.buildTutorials.repoTourPath`; runtime dep `repo-tour: file:../repo-tour`.
- repo-tour (main, after T-11/T-12/T-13): `repo-tour/build` → `buildPlan(digest, { root, witness })`, `interpretPlan(plan, digest, opts)`, `check`, `stubFile`, types; `repo-tour/digest` → `digest(root, opts)`; `repo-tour/skins` → `SKINS`, `DEFAULT_SKIN`, `baseCss()`, `alternateCss()`; `repo-tour/extract` → `initParsers`; `repo-tour/interpret` → `InterpretOptions`, `defaultCacheDir`. Schema: `repo-tour/schema/build-plan.schema.json`.

## Touched set
- `src/core.ts` (edit) — add `'build'` to `CORE_MODULE_NAMES` (doctor then reports 8 modules; update its expected count and the T-2 test that asserts 7).
- `src/skins.ts` (new) — the extension side of the skin contract: `bridgeCss()` (base tokens → `--vscode-*` under `:root:not([data-theme])`), `readSkin(globalState)`, `writeSkin`, `pickerHtml(SKINS)`.
- `src/webview/page.ts` (new) — `renderPage({ title, body, script, skin, csp })`: `<!doctype html>` with a nonce CSP, `<style>` = `baseCss() + alternateCss() + bridgeCss()`, `data-theme` stamped on `<html>` by the host (no flash), `acquireVsCodeApi()` bootstrap, `getState/setState` helpers.
- `src/start/request.ts` (new) — `BuildRequest { idea; recreate; repo; target; dial }` + `validateRequest(req): { ok; problems: Record<field, string> }` (pure; unit-testable without VS Code).
- `src/start/panel.ts` (new) — `StartPanel` (webview panel, editor tab, `retainContextWhenHidden: false` + state restore): the form, the message protocol (`form:changed`, `form:submit`, `pick:repo`, `pick:target`, `skin:set`), the idea-only honesty line.
- `src/start/build.ts` (new) — `buildFromRequest(req, core, ctx, progress)`: clone-or-path, language refusal, the write-ahead marker, digest → `buildPlan` → `interpretPlan`, `plan.json` + `request.json`, cost notification, `resumeIfMarked(ctx)` on activation.
- `src/extension.ts` (edit) — `start` opens `StartPanel`; activation calls `resumeIfMarked`; new settings read.
- `package.json` (edit) — settings `buildTutorials.llmProvider` (default `claude`), `buildTutorials.llmModel` (default repo-tour's `DEFAULT_MODEL`), `buildTutorials.skin`? — no: skin lives in `globalState`, not settings (matches repo-tour's localStorage choice).
- `esbuild.mjs` (edit) — no new bundle needed if `test/` grows within the same entry; add `test/unit/*.test.ts` bundle if pure unit tests run under plain mocha (recommended: `npm run test:unit` = mocha on `dist/test/unit`, no VS Code).
- `test/extension.test.ts` (edit: module count), `test/unit/request.test.ts`, `test/unit/page.test.ts` (script parse guard), `test/start.test.ts` (vscode-test: end-to-end on a fixture repo with `cachedOnly`).
- `README.md` (edit: the start screen), `.gitignore` (`.scratch/` already).

## DO-NOT-TOUCH
`src/doctor.ts` beyond the module count · `scripts/*` · `.github/workflows/ci.yml` (T-2's, green) · repo-tour's tree · `.autodev/**` ledger.

## Conflicts
None in this shop (T-4 … T-8 all wait on this). Cross-shop: consumes repo-tour `main` at or after T-13's merge — do not start before it.
